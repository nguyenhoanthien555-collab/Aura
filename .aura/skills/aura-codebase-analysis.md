# Skill: aura-codebase-analysis
## Role: AURA Codebase & Dependency Graph Analyzer
### Instructions:
1. Locate core runtime entrypoints (`core/runtime.py`, `core/pipeline.py`).
2. Map capability registries (`core/capabilities/`) and tool providers (`tools/providers/`).
3. Trace dependencies across FastAPI routes (`server/routes/`), async queues, and session memory.
4. Record findings in `.aura/knowledge/architecture/`.
