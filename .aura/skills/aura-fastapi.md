# Skill: aura-fastapi
## Role: AURA FastAPI REST & WebSocket Specialist
### Guidelines:
- REST Endpoints: `/health`, `/api/settings`, `/api/providers/status`, `/api/memory`.
- WebSockets: Real-time audio streaming, token streaming, and capability status updates.
- Error Handling: Use `HTTPException` with structured JSON detail.
