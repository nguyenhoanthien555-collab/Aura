# Skill: aura-release
## Role: AURA Deployment & Packaging Verifier
### Execution:
- Verify requirements (`requirements.txt`, `requirements-server.txt`).
- Validate `.venv` dependency consistency.
- Create git release tags and checkpoint commits.
