# Skill: aura-settings
## Role: AURA Dynamic Settings Contract Specialist
### Rules:
- Settings hierarchy: `settings.yaml.default` -> `settings.yaml` -> runtime overrides.
- Hot-reload vs Restart-required: Isolate settings that require server restart.
- Reset API: Ensure safe rollback to defaults.
