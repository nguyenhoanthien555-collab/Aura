# AURA JARVIS Modernization — Implementation Checklist

Baseline established 2026-08-23:
- Backend: `1817 passed, 1 skipped, 1 deselected`
- Android: `312 tests, 0 failures, 0 errors, 0 skipped`

Status vocabulary is exactly: NOT IMPLEMENTED / PARTIALLY IMPLEMENTED / IMPLEMENTED.
"Audited", "designed", "planned", "documented" are NEVER recorded as IMPLEMENTED.

| # | Phase | Status | Notes |
|---|-------|--------|-------|
| 1 | Provider / credential foundation | IMPLEMENTED | `brain/providers/custom.py` `CustomProvider`: OpenAI-compatible, no default URL and no default model, `requires_base_url = True`. Key and endpoint resolve in one constructor and nothing in the request path reads the model *name*, so a `claude-*` model on the owner's gateway cannot redirect to Anthropic. `base_url` ctor param carries `llm.custom_base_url` (setting > env > default). `OWNER_DEFINED_ENDPOINTS` in `brain/router.py` routes absent endpoint/model through `_skip_reason`, which names the empty setting instead of "initialization raised ValueError". `_endpoint_url` validator requires an explicit scheme (guessing http vs https would risk sending the key in cleartext); loopback and private hosts accepted. External input still required: the owner's AgentRouter base URL. |
| 2 | Unified Model Contract | IMPLEMENTED | `brain/capabilities.py`: `TaskClass` (all 9 mandated classes), pure `classify_task`, `CapabilityLLM` protocol + `generate_for` free function. Wired at the single `_generate` seam in `brain/conversation.py`; task decided once in `_prepare`, carried on `_Turn`. |
| 3 | Model Router 2.0 (capability) | IMPLEMENTED | `brain/model_router.py` `CapabilityRouter`: lane per task class, lazy build, dead lane degrades to chat, chat lane is the floor. `llm.task_models.*` in `core/config.py` + `core/settings_store.py` (5 lanes). Composition root `launcher/services.py` wraps only when a lane is configured. Android `TaskModelsDto` reads them back. |
| 4 | Cognitive State | IMPLEMENTED | `core/cognitive.py`: `CognitiveState` (mutable owner) + frozen `CognitiveSnapshot`/`ActionRecord`/`Focus`, the same bargain `ProactiveContext` makes. Tracks session, owner, conversation, intent, goal, plan, task node, focus (application+screen), active tools, and every action as pending/succeeded/failed with an attempt count and recovery scope. Time is borrowed from an injected `TemporalClock` and never stored, so no seventh source of "now". An action is identified by `(kind, target)`, so `has_succeeded("open_app", pkg)` is a lookup; `begin_action` on succeeded work is a no-op unless `enter_recovery` named it, which is the invariant that kills the open_app loop. `CognitiveStore` keys one state per session behind a lock (a field on the shared `ConversationManager` is "a race, not a cache" per its own comment) and sweeps idle entries touch-before-sweep, so a live task cannot expire under its reader. `brain/agent_mode.absorb` ingests the tick's app/screen/request/completed_actions using only the `kind(args) [VERIFIED]` format `formatActionHistory` emits and `AccessibilityAgentTest` pins; `last_action_error` is deliberately not parsed (five free-prose shapes, no recoverable target). Wired at `_prepare`'s machine branch, store built once in `launcher/services.py`. Consumers (planner, task graph, prompt) are phases 5-7 and 17. |
| 5 | Planner | IMPLEMENTED | `brain/planner.py`: pure module, no class, following `brain/capabilities.classify_task`'s precedent. `StepKind` (OPEN_APP/FOCUS_SEARCH/ENTER_QUERY/SUBMIT_SEARCH/AWAIT_RESULTS/SELECT_RESULT), frozen `PlanStep`/`Plan`, `plan_for`, `search_query`, `current_step`, `render_plan`. The plan's *structure* is recomputed each tick rather than serialised - safe because `plan_for` is pure, so two call sites cannot disagree and no second copy of progress exists (S8). Position is read from `CognitiveState.succeeded`, never a step counter. `AWAIT_RESULTS` shares its evidence with `SUBMIT_SEARCH` because the device verifies a submit by polling until content changes, so a verified submit *is* results rendered; inventing an `await_results` action would have left the step permanently unmet. `click` satisfies SELECT_RESULT and deliberately not FOCUS_SEARCH, or tapping the search box would complete a selection plan. Rendered as a new `PLAN` section between COMPLETED ACTIONS and AGENT RULES via a `plan=` param on `build()` - the manager computes, the builder places. `CognitiveState.set_plan`/`enter_node`, dead surface since phase 4, are now written by `ConversationManager._plan`. Unplannable request -> empty plan -> no section -> byte-identical prompt. |
| 6 | Task Graph | IMPLEMENTED | `brain/task_graph.py`: `NodeState` (all 7 mandated states, `str`-valued lowercase like `ActionState`/`TaskClass`), frozen `TaskNode`/`TaskGraph`, `build`, `current_step`, `render`, `render_plan`. **Node state is a projection, not a store** - every state is derived on demand from `CognitiveState` + `Plan`, so S8 is not violated and `set_plan`/`enter_node` needed no change (the open question from phase 6's plan: nothing had to give). Precedence RECOVERING > SUCCESS > SKIPPED > RUNNING > FAILED > BLOCKED > PENDING: RECOVERING beats SUCCESS because that *is* S10's exception (if SUCCESS won, `enter_recovery` could never reopen a node and the exception would be decorative); SUCCESS beats SKIPPED because after a real launch both facts hold and SKIPPED would deny we did it. **SKIPPED is the state that changes hardware behaviour today**: `absorb` already records the foreground package, so a plan whose app is already open now renders `[SKIPPED]` and advances instead of emitting another `open_app`. Only OPEN_APP may claim it - `focus.screen` arrives permanently empty (the `AppInfo.activity` defect), so no other step has evidence and none is allowed to pretend. BLOCKED is about *successors*: an exhausted node is FAILED, the steps after it BLOCKED, and `is_stuck` vs `is_finished` disambiguate `current is None`. The retry bound is not re-implemented - `should_retry` is asked, keeping one retry accounting (the Android side's `"${action.action}:${action.nodeId}"` key already disagrees with Python's `(kind, target)`; one more would be worse). No `depends_on` edge list: `plan_for` yields a chain, so a field always equal to `(index-1,)` would be structure pretending to carry information. `current_step`/`render_plan` **moved here from `brain/planner.py`** with their 22 tests so there is one implementation of "what state is this step in"; planner keeps parsing plus the now-public `is_done`/`same_app`/`describe`/`SATISFIED_BY`. Honest reach: PENDING/SUCCESS/SKIPPED occur in production now; RUNNING needs `begin_action` and FAILED/BLOCKED/RECOVERING need `fail_action`/`enter_recovery`, which phase 7 owns - the projection is complete and tested for all 7, three of its inputs arrive later. |
| 7 | Verification + Recovery | IMPLEMENTED | The gap was **not** missing verification: `AuraActionExecutor` already does the real per-kind check with bounded polls (`waitForForegroundPackage`, `waitForContentChange`, fingerprint compare), and rebuilding it server-side would be a second verification system whose disagreements with the first are invisible. The gap was that the *result* never crossed the wire readably - `completedActions` is appended only on `ExecutionResult.Verified`, and failure travelled only as `last_action_error`, free prose in five shapes with no `(kind, target)` - so `fail_action`/`should_retry`/`enter_recovery` had sat in `core/cognitive.py` since phase 4 with **no production caller** and FAILED/BLOCKED/RECOVERING were unreachable. Closed by a sibling wire field: `AccessibilitySnapshot.failedActions` carries `open_app(com.android.chrome) [FAILED x2]` in the format `completed_actions` already uses, defaulted so an older APK still deserialises. Verdicts are the device's own `ExecutionResult` names (FAILED = could not perform, UNVERIFIED = performed, postcondition unobserved), which is S11's distinction rather than an invented taxonomy. `brain/recovery.py` holds policy: `DEFAULT_RETRY_LIMIT` = 2 because the service refuses to execute past `MAX_ACTION_ATTEMPTS`, so that number *acts* and a server bound above it would be permission the phone declines - pinned across languages in `test_agent_protocol.py`. `RETRY_LIMITS` deliberately **empty**: every entry would need justifying and today every action is a UI gesture whose ceiling the service sets; `limit_for` is total (unknown-means-unlimited is S12's forever-repeat) and never zero (a block wearing a retry count would be S2's arbitrary restriction; refusals belong in `SafetyGuard`, visibly). `invalidated` re-asks S11's launch postcondition every tick - launches only, because `focus.screen` is permanently empty. **Recovery entry is gated on `is_stuck`, and a regression caught the flaw**: one foreground package cannot distinguish a killed app from one behind a permission dialog/share sheet/sub-activity, so an ungated reconciler relaunches healthy apps every tick - manufacturing the exact `open_app open_app open_app` loop S10 exists to prevent. Closing stays ungated (must fire on the tick the app returns; a plan in recovery is never stuck by construction). Recovery is itself bounded, checked with `attempts_for` not `may_retry`, since `may_retry` refuses succeeded actions and the recovered action is succeeded by definition. Absorption is idempotent (counts brought *up to* the report, not added) and bounded by arithmetic, because `begin_action` returns a finished record without incrementing when the action succeeded, so a `while` form would never end. Successes read before failures: the device clears a failure count on verification but a tick in flight can still carry the stale line. `Blocked` deliberately not reported - a safety refusal is not a thing to retry. **Two real defects fixed**: `actionKey` read `action.nodeId` for every kind, so every launch keyed to the literal `open_app:null` and two failed Chrome attempts refused YouTube's *first*; and the three dead producers now have tested callers. 57 new Python tests + 2 cross-language pins + 11 Kotlin; `2152 passed`, Kotlin `330 tests, 0 failures` after clean, `assembleDebug` successful. **Not** verified on hardware. |
| 8 | Persona Engine + Validator | IMPLEMENTED | The **engine already existed** - `brain/persona.py` (PronounStyle, read_style, resolve, PersonaDials, AddressPreference, ContextMode) was wired through PromptBuilder -> ConversationManager -> ChatEngine -> config as a per-turn PERSONA section before this mandate, so it was extended, not replaced (untouched by this phase). The gap was the half S13 and S14 name in the same words - "prompt instructions alone are insufficient": the prompt stated the register and *hoped*, so a model that agreed to cau/to then wrote "may" in paragraph three reached the user uncorrected **and** was saved to the transcript, feeding the drift back next turn as an example of how Aura talks. `brain/persona_validator.py` is the consequence: one public `validate(text, state)`, scope **exactly** the promises `_pronoun_line` and `RESTRAINT` already make and not one word wider, because enforcing an unstated rule corrects a model for obeying its instructions. Four coarse words -> the register's terms (self vs address kept distinct: swapping "tao da sua"/*I fixed it* for "cau da sua" says **he** did, a changed fact); a forbidden word **dropped, not swapped** (inventing a replacement guesses at a preference he never stated - the same bargain `PersonaState.address_word` documents); emoji runs keep their first, ZWJ sequences consumed whole so a joined emoji is not "collapsed" into half of itself; an address term in *every* sentence past three keeps its first (two is emphasis, and policing emphasis would correct a reply that kept its promise). **The one substitution exception is justified, not waived**: `style.py` declares "never substitutes, never reorders, never paraphrases" absolute and that rule stands - so substitution lives in a separate module, and the difference is one of kind, a closed vocabulary of eight words swapped for another member of the same set chosen by `resolve`. **Targets come from the resolver, never a constant** - an owner in tui/bro gets "Bro thu lai di", and `test_the_target_words_come_from_resolve_not_from_a_constant` asserts against `resolve` so the pin cannot rot into a tautology. **S2 got a mechanism**: `address.preferred` is checked before any correction, so an owner who asked for "may" is called "may". Code and quotes protected **by construction** - `style._hide_code`/`_restore_code` promoted to public and reused rather than reimplemented, since two answers to "what counts as code" would surface as one layer editing a snippet; `tao_count` and a quoted "tao xong roi" both survive. Three linguistic guards all biased towards **under**-correcting: "ong"/"ba" corrected only in vocative position ("ong ay" = he, "ong noi" = grandfather - guessing wrong the other way costs the sentence's meaning), "minh" left alone after cua/tu/chung, and diacritics do the rest for free since "tao" (create) and "may" (lucky) are different strings - tested, because "happens to work" and "is guaranteed" read identically in a diff. **Machine-turn exemption is structural**: a machine turn builds no `_Turn`, so persona is None and JSON returns verbatim - a threaded boolean would be one mistake from rewriting an action's field names. Validation runs **after** styling, because style can delete a clause and that changes what "every sentence" means. **Two `RESTRAINT` sentences deliberately unenforced**, reasons in the docstring: the repeated-opener rule has no safe subtractive fix (deleting stance-carrying "Khong -" inverts an answer; `style.py` already strips the stance-free openers unconditionally, so it would add nothing where it is safe), and "never reach for a trend word" cannot be judged without the surrounding sentence. **One S7 over-reach declined**: stripping model self-identification would catch a real violation but also delete an honest answer about the owner's own configuration, which S2 and S28 weigh against more heavily. 49 new Python tests (10 classes, red first - `ModuleNotFoundError` at collection, then 44/49, then 49/49 after wiring), five of which exist only to check the validator does **nothing**; `2201 passed, 1 skipped, 1 deselected` from a 2152 baseline, zero regressions. No Kotlin changed, so **no Android rebuild** - phase 7's APK stands. |
| 9 | Conversation Persistence | IMPLEMENTED | The inspection had to come first, because S15's one sentence ("AURA's chat UI must NOT lose visible history when the application closes") does not say where history should live. The server side already persisted - `ConversationStore` is what `_remember` writes and `history()` reads, keyed by session - so the question was what the *phone* did, and the answer was nothing: `ChatViewModel._state` was seeded with an empty `ChatUiState()`, `AuraApi` has no history route, and there was no local store. **The half that mattered more was invisible**: `AuraRepository._sessionId` was an `AtomicReference` that died with the process too, so restoring the bubbles from anywhere *without* the id would have shown a transcript beside a server session that had never heard of it - worse than a blank screen (S38). **Local, not a server round trip**: a history route would need both sides, and even built, a launch-time fetch shows an empty transcript when the phone is offline - failing S15 precisely where losing history hurts. Four new files: `data/chat/Transcript.kt` (the contract), `data/chat/TranscriptCodec.kt` (pure `object`, no Android type, so encode/decode/bound are JVM-testable), `data/chat/TranscriptStore.kt` (the one implementation), `ui/chat/ChatTranscript.kt` (the mapper, in `ui` because `data` may not import `ui`). **Two narrow interfaces over one store**, copied from the shape already here - `SettingsProvider`/`DeviceSettings`/`SettingsStore` - for the same reason: the real store needs a `Context` and a Keystore key, so a JVM test dies in its constructor. `Transcript.None`/`SessionStore.None` are constructor defaults, which is why every pre-existing call site and test compiled untouched. **`EncryptedSharedPreferences` + `kotlinx.serialization` because both are already declared** - the build runs `--offline`, so a new Gradle dependency may not resolve - in its **own** preferences file (`aura_transcript`), since per-turn transcript churn must not re-encrypt the URL and API token on every message; same `MasterKey` and schemes as `SettingsStore`, because the transcript is the most personal thing the app stores and the key already existed. **Three guarantees structural, not remembered**: `StoredMessage` has no `streaming` column so a half-arrived reply cannot come back looking live (asserted against the *encoded text*, so adding the field later fails the test); `keep()` returns early on an empty projection, so emptiness is only ever written by `newConversation()` - every other route to an empty screen is a failure, most sharply a read that threw, and writing it back would **destroy** the transcript rather than fail to load it (S41); and `adopt()` is now the sole writer of the session id, where `send()` used to `_sessionId.set(dto.sessionId)` directly - recorded consequence, a *blank* id from the server is no longer adopted. **`decode` never throws at all**: it runs in `init` before the screen draws, so an exception is not a lost transcript but an app that cannot open; corrupt JSON, a non-object root, a non-array `messages`, an unparseable row and an unknown author all degrade, and an unknown author is **dropped rather than guessed** because rendering it as AURA would show the user something Aura never said. Built as a JSON *tree* rather than `@Serializable` so one bad row does not take the rest. **Four catches, each with a stated cost**: nullable `prefs` (an invalidated Keystore must not stop the app starting), `restored()` (above), `keep()` (a failed write leaves `kept` alone so the next change retries), `AuraRepository.remember()` (losing continuity at the next launch beats losing the message being sent now, which the user is watching). All logging is the exception's class name only - no contents, no key material (S30). **Streamed replies are written once, not per token**: the projection filters `streaming` messages, so it is constant for the whole of an arriving reply and `distinctUntilChanged` emits when it settles. **Two decisions not to delete data**: `SettingsViewModel.save()` and `disconnect()` clear the session id but **keep** the transcript, because it is what the user said and tidying an inconsistency they can see for themselves is not worth destroying it (S41), and "new conversation" is one tap away; `Transcript.clear()` clears messages only, since forgetting the session is `remember(null)`, owned by the repository. **Recorded loss**: `MAX_MESSAGES` = 200, applied by the codec rather than the caller so no call site can forget it - past that the oldest messages stop surviving a restart. **A defect found in this phase's own code**: the `keep()` collector's first emission is the conversation `restored()` just loaded, so every launch would re-encrypt up to 200 messages to write back what was already on disk - fixed by a `kept` field and a `messages == kept` guard. That fix introduced a second one: `kept` declared *after* `keep()` is read before its initialiser runs on `Dispatchers.Unconfined` (a `launch` body runs synchronously) and then clobbered by it - fixed by moving the declaration above `init`, with a comment, pinned by `a launch does not write back what it just read`. **Red was produced deliberately** since tests and implementation were written together: removing the emptiness guard and making `restored()` return `emptyList()` -> **6 of 10 failed**; removing the `kept` equality guard -> **1 of 11 failed**, exactly the ordering test. Both reverted and re-confirmed green (`grep -c MUTATED` -> 0). 29 new Android tests (14 codec, 11 persistence, 4 repository); `classes=26 tests=359 failures=0 errors=0 skipped=0` from a 330 baseline, freshness proved by reading the JUnit XML `timestamp` attributes (`04:40:44`-`04:40:46`) against `date -u` (`04:42:32`) rather than trusting BUILD SUCCESSFUL (S34). APK rebuilt; its byte size matched the previous two builds exactly, which was checked rather than glossed over - opened as a zip, 12 dex files, `TranscriptStore` present in `classes4.dex` and `classes6.dex`, so the build is genuine and the size match coincidence. No Python changed: `2201 passed, 1 skipped, 1 deselected`, unchanged. Still owed: the APK has never been installed on hardware, and S35 gains one scenario worth driving - send a message, force-stop, reopen, confirm both the bubbles and the session id came back. |
| 10 | Time Awareness | IMPLEMENTED | S16 sets two prohibitions pulling opposite ways - "Never rely on the model guessing the current time" and "Do not hard-code dates" - and `core/temporal.py` (`TemporalClock`, `TemporalContext`, `resolve_timezone`, `describe`, the `TIME` prompt section) **already existed and predates this mandate**, so a second clock would have been the seventh source of "now" S8 forbids by name. The gap was **reach, not absence**, in two halves. **Gap 1: the CLI root had no clock.** `core/app.py::Aura.__init__` loaded the config and then built `ChatEngine()` bare; `ChatEngine` leaves `clock=None` deliberately (a bare engine is byte-for-byte the Sprint 4 prompt pipeline many tests depend on), so a faculty gated on the clock arrives from a composition root or not at all. `launcher/services.py` always builds one, so the *server* was fine - but `python main.py` held whole conversations whose prompt had no `===== CURRENT TIME =====` section, and a model with no date in its prompt does not decline to answer "what day is it", it invents one. Fixed at the root only, leaving the documented `clock=None` default untouched. **Gap 2: `temporal.timezone` was readable and unsettable.** The key was in `core/config.py`, read by `TemporalClock.from_config`, and `effective` had always carried its value to the phone - but it was absent from `ALLOWED`, so the phone could display the zone and never change it: the dead control S2 rules out, biting hardest in the deployment the key was written for (a container in UTC whose owner is not, with no `config.yaml` to edit). Now settable, live-applied, and refused-with-a-reason when unresolvable. **`use_timezone` mutates in place rather than returning a new clock, and that is the design.** `launcher/services.py` hands *one* clock object to the prompt builder, memory pipeline, ranked retriever, quiet-hours check and proactive engine so "the time in the prompt and the time on a stored memory cannot disagree"; a replacement would move whichever subsystem received it and leave the rest on the old zone - the exact disagreement a shared clock exists to prevent. The default `_now` closure reads `self.timezone` at *call* time, so even `RankedRetriever`, which captured the bound `clock.now` at construction, follows (pinned). **Constructor and setter disagree on purpose:** a bad name at construction degrades to system local (refusing at startup would mean no Aura over a typo); the same name through `use_timezone` is refused and nothing changes (a running clock has something strictly better - the zone already in effect - and silently dropping it would punish a typo by moving Aura's clock). An injected `now` outranks the zone and is left alone; the label still moves, because the label is what the prompt prints. **The validator refuses rather than stores** an unresolvable zone: storing it would leave the settings screen showing a zone the clock never uses, which is the dead setting `validate_path` refuses by name, while refusing costs one error message and leaves the owner's existing value standing. Nothing is silently mutated either way (S2). The message names `tzdata` because on Windows the cause is usually a missing database rather than a typo, and "unknown timezone" alone would send the owner hunting a spelling mistake that is not there. **One vocabulary for the aliases:** `UTC_ALIASES` extracted so `core/settings_store.py` cannot keep a divergent copy; `canonical_timezone_name` folds whitespace and the aliases *only*, because IANA keys are case-sensitive to `ZoneInfo` (`asia/ho_chi_minh` resolves nowhere) while leaving `"utc"` unfolded would print `(utc, UTC+00:00)` in the prompt. `server/settings_service.py` gains `LIVE_PATHS` + `_reapply_temporal`, which returns False - i.e. `restart_required` - when this process has no clock or the clock refuses the name. **Recorded caveat, not a blocker:** timestamps are stored naive local, so a zone change re-dates existing memories by the offset delta; identical whether applied live or at restart, therefore not an argument for demoting it. **No route change was needed** and it was confirmed by test rather than by reading: `SettingsPatch` carries `settings: Dict[str, Any]` because "the allow-list in `core/settings_store.py` is the real schema", so `{"temporal": {"timezone": "UTC"}}` over the wire returns `applied == ["temporal.timezone"]`, `restart_required == []`, and the next GET reports it; `Mars/Olympus_Mons` returns 422 with `tzdata` in the body and leaves `effective["temporal"]` untouched. **Host limit, honestly recorded:** this Windows box has no tzdata, so only `UTC`/`GMT`/`Z` resolve. Tests therefore never pin a specific non-UTC zone's outcome (`UTC` to accept, `Mars/Olympus_Mons` to refuse), and `test_a_zone_is_not_lowercased` calls `pytest.skip("no system timezone database")` rather than pretending to pass - that is the 1 new skip. On this host the owner can only set `UTC`; `pip install tzdata` lifts it. 24 new Python tests (22 written and confirmed red first - `AttributeError: 'TemporalClock' object has no attribute 'use_timezone'`, 5 failed / 81 passed in `test_temporal.py` alone - plus 2 end-to-end HTTP tests added during self-review), including `tests/test_app_wiring.py`, which is new because `core/app.py` had **no test file at all**. `2225 passed, 2 skipped, 1 deselected` from a 2201 baseline, 0 regressions. Android: `ALLOWED` 49 -> 50 mirrored in `SettingsContractTest.kt` with the path named, `classes=26 tests=359 failures=0 errors=0 skipped=0` freshly executed. **A S30 violation was introduced and repaired in this phase** - see the verification log and `progress.md`: `AURA_WRITE_ANDROID_FIXTURES=1` rewrites all three live fixtures from the host's live state, and a real Gemini key in `.env` put the masked tail of the owner's key into `providers.json` and `settings.json`. Repaired by regenerating through a keyless runtime (not `git checkout`, which S45 forbids and which would also have reverted phase 1's `custom` provider additions); all three fixtures are now additions-only vs HEAD and grepping the key's tail across them returns nothing. Phase 23 UI debt grows by one: no Hub widget for `temporal.timezone`, joining the five `llm.task_models.*` lanes and `llm.custom_base_url`/`llm.custom_model` - eight settable-but-unwidgeted paths, deferred per S32. |
| 11 | Memory 3.0 | PARTIAL | **Part 1 done: the owner control, which was dead.** `memory.recall` had four independent breaks and reached nothing - see the verification log for all four and for the S2 semantics conflict (`config.yaml` says keyword search, the Hub says "use memory in replies" under privacy; the phone governs). **Confirmed against the repository before designing anything:** S17's *"do not blindly save every conversation line as permanent memory"* is **already satisfied** - `MemorySelector` applies six hard rejections, a first-person requirement and a scored threshold (`DEFAULT_THRESHOLD` 0.5, six weighted signals) before anything is stored, and the unconditional both-sides transcript write is S15's *visible history*, a different guarantee that must not be "fixed". **Part 2 done: the semantic tier can now learn at runtime** - `remember` (`tools/builtins/memory.py`) is its only door, SAFE so it survives server mode's no-human rule, registered only when a pipeline exists, and driven end to end through the real conversation loop into a real database. **Part 3 done, and it fixed a S10 defect rather than adding a tier.** Looking for a real procedural fact to learn surfaced that `same_app` missed 7 of 10 common apps, making `is_done` False for launches that happened and re-issuing OPEN_APP forever; `APP_ALIASES` (exact packages, whole-name keys) closes it in one place for all six call sites. The **learned** procedural tier is **Not Implemented** and said so plainly (S44): plan caching buys nothing because `plan_for` is deterministic with its signature pinned to `["request"]`, a module-global resolver contradicts the composition-root idiom (`set_tool_confirmation` is a method on the runtime), and the honest version needs the device to report which package it launched - a contract change requiring S35 hardware this host does not have. **Still owed on the tiers.** `episodic` exists and is good (`EpisodicStore` + `RankedRetriever`). `working` exists unnamed as `TemporaryContext` (3h TTL, 12 entries, never persisted). `semantic` is reachable at last through `user_model`, but still fragmented across two other unrelated things called "fact" - `user_facts` (CLI-write-only) and `companion.Fact` (never written in production). `procedural` still does not exist as a tier; `procedural`, `consolidat*`, `decay`, `salience` are zero matches repository-wide. Nothing decays, consolidates or promotes between tiers, by documented design (`memory/temporary.py`, `memory/user_model.py`, pinned by `tests/test_memory_2.py`). Not claimed as implemented (S44). |
| 12 | Event Bus | DONE | **The bus was already correct and had nobody listening.** `events/bus.py` had the lock, the snapshot-inside/dispatch-outside discipline, handler exceptions swallowed into `logger.exception` and base-class subscription reaching subclasses - and **zero production callers of `subscribe_all`**, while `events/__init__.py` and `subscribe_all`'s own docstring both named logging as its user. Cost, concretely: a proactive notification that never reached the phone left no trace to separate *never published* from *aged out of the outbox* from *drained by another device* - one symptom, three bugs. **Part 1:** `events/log.py`, the first observer, attached unconditionally in `launcher/services.py` before anything can publish (not behind a flag - it writes at DEBUG, `logging.level` is already the owner's switch, and a second switch could disagree with the first, S31). Its whole design is S30: **strings denied by default**, written `<N chars>` unless the owning event opts the field in via `log_fields`; `SAFE_FIELDS` names the six meant for humans; containers named but never walked, which keeps a tool's `arguments` out without a per-tool blocklist; **Enum tested before `str`** because `AuraState`/`Mood`/`Expression` are all `(str, Enum)` and would otherwise redact `state=thinking` to a character count, `bool` before `int` for the same reason; `QUIET_EVENTS` drops `BlinkEvent` and `StreamChunkEvent` by `isinstance` so subclasses inherit the exclusion; `ErrorEvent` at WARNING, the rest at DEBUG. **Part 2:** phases 4-11 - state, planner, task graph, verification, recovery - published **nothing at all**, so the one thing worth watching was the one thing invisible. `TaskStepChangedEvent` / `TaskFinishedEvent` / `TaskStuckEvent` now publish from `_announce`, **edge triggered**: `plan_for` is pure and the graph is rebuilt every tick, so a per-tick publish would emit the same step forever and put S10's repeat loop on the bus as noise. Finished and stuck are both "no current node", which is why `graph.current` cannot be the signal - a subscriber confusing them would congratulate the owner on a search that never happened. |
| 13 | Background Service | DONE | **The device side already satisfied S19; the owner's limits did not survive a restart.** Verified before building: `NotificationWorker` is periodic at WorkManager's 15-minute floor with `NetworkType.CONNECTED`, `ExistingPeriodicWorkPolicy.KEEP`, `Result.retry()` backoff, and it **re-checks every gate per run** rather than trusting what was true at scheduling time; `brain/agent_mode.absorb` re-sends the whole action history each tick, so a server restart mid-plan loses nothing. No Kotlin needed changing, and S19's **"Do NOT pretend an Android application can bypass OS restrictions"** is the reason the phase was not redefined into building a scheduler that never dies. The honest target is surviving being killed - and **five owner-configured proactive limits did not**: `max_per_day`, `cooldown_seconds`, `category_cooldown_seconds`, `duplicate_window_seconds` + `similarity_threshold` were every one of them derived from a single RAM-only `deque(maxlen=64)`, so a restart handed back a clean slate and "no more than four a day" was a number in the settings file that nothing enforced (S2, S19, S20). `proactive/ledger.py` is the fix, built on the `core/settings_store.py:620-670` pattern - `{"version": 1}` root, unreadable file logged as `type(error).__name__` only, non-dict root ignored, per-row validation dropping the offender rather than the file, atomic `.tmp` + `os.replace` - diverging in one documented place: `save` catches its own exceptions, because a failed write of a rate-limit record must not take down the send. A JSON file rather than a fourth memory table because `memory/models.py` states its three tables are "one per kind of knowing" and deliberately has none for temporary context, "the whole point is that it cannot silently become permanent". It stores **time, category and message text**: the text is not detail, `duplicate_window_seconds` and `similarity_threshold` are questions about *what was said*, so timestamps alone would have restored four limits and silently dropped the fifth. Threaded as `ProactivePolicy(ledger=None)` / `build_proactive_engine(ledger=None)` and supplied only at the composition root, which is the reasoning `core/app.py` already records for the clock; loaded once in `__init__` because `allows` asks the history four questions under one lock and per-question re-reads could disagree inside one decision. **`ProactiveEngine._greeted` deleted, not persisted**: `part_of_day` is pure, so "did I greet this part of the day" is derivable from the ledger - the dict was an S8 duplicate of a fact already recorded *and* the copy that died, so deriving it removed the duplicate and stopped post-restart re-greeting in one move (`_prune_greetings` went with it). **The fix then exposed its symmetric half**, found by probe: `_last_user_message_at` is volatile, `seconds_since_user()` reads missing as infinity, and the greeting rule reads infinity as "away" - so a restart mid-conversation greeted a present owner (S21). Presence now derives from the `messages` table via the new `MemoryManager.last_said_at`, with `note_chat()` kept as a narrower-lifetime cache rather than a parallel truth. **A third volatile field was checked and left alone with evidence**: `_rotation` resets to 0 and re-offers identical text, which the now-durable duplicate window refuses - the guarantee moved to the layer that can keep it. Proved end to end, not asserted: 4 sends -> a 623-byte `data/proactive.json` -> restart -> `sent_today: 4`, fifth refused ("daily limit reached (4)"), and tomorrow allowed, because a persisted ledger must not become a permanent ban (S2 - Aura may warn, not lock the owner out). `data/proactive.json` and its `.tmp` sibling are gitignored: not a secret, but a record of what Aura said to this owner unprompted, written by the running server. |
| 14 | Notifications | DONE | **The gate that decides whether Aura speaks first could not count past a restart, and the owner could only switch it off.** S20 is "do not spam notifications", which is a claim about *rate*, and `companion/policy.py` kept its whole history in a RAM-only `deque(maxlen=32)`: every one of `cooldown_seconds`, `max_per_hour`, `suppress_after_chat_seconds` and the duplicate check was derived from it, so restarting the server handed back a clean slate and "no more than N an hour" was a number in the settings file that nothing enforced across a bounce. Worse for S2, the duplicate window was `DUPLICATE_WINDOW = 1800.0`, a **hard-coded constant sitting beside four real settings** - the owner could tune when Aura may repeat herself only by editing source. And of the six knobs the sibling proactive gate exposes, `server.companion.*` exposed exactly one, `enabled`: an owner who finds her chatty had one remedy, silence, and nothing between that and the default (S2 - "do not create arbitrary restrictions that prevent the owner from changing their own configuration"). The fix **reuses `SendLedger` from phase 13 rather than cloning it** (a repository search came first, per CLAUDE.md), but deliberately not its file: `LEDGER_PATH = DATA_DIR / "companion.json"` with `LEDGER_CATEGORY = "companion"`, because one shared ledger would make each gate count the other's sends and the owner configured *two* budgets, not one. Time is handled the way phase 13 established and phase 10 requires: monotonic for intervals, wall time only to cross the disk, and **ages that come back negative are discarded rather than trusted** - a laptop resumed from sleep, an NTP correction or a timezone change must not wedge the gate shut, and a test drives `wall.advance(-3600.0)` to prove it. `DEFAULT_DUPLICATE_WINDOW = 1800.0` keeps the old constant's value as the default only, and all six paths are now settable and live-reapplied: `server.companion.enabled`, `relevance_threshold`, `cooldown_seconds`, `max_per_hour`, `quiet_hours`, `suppress_after_chat_seconds`, `duplicate_window_seconds`. `_last_notified` was **deleted, not persisted**: it was an S8 duplicate of a fact the history already holds, so `allows()` now derives it with `max((when for when, _ in recent), default=None)` - one truth, and the one that survives a restart. `reset()` clears the file as well as the deque, because a limit that comes back from disk after the owner dropped it is not a limit the owner controls (S2). The `NotificationOutbox` was re-examined during this phase and **deliberately left alone**: the outbox is a queue, not a receipt, and rate limiting has to be decided by what was actually *sent*. **Mutation testing found a real class of weak test and cost two rounds.** Round one caught 19 of 28; the survivors clustered in the age arithmetic (a stale row that should have aged out still occupying a live slot in the 32-deep deque, a duplicate not matched after hand-editing whitespace into the ledger, a cooldown measured from the oldest send instead of the newest) and, after those were closed, **two survivors in the live-reapply path** - which survived because the tests asserted *membership in `LIVE_PATHS`* rather than driving a real PATCH and reading the running policy afterwards. That is the identical shape to the phase 11 `memory.recall` defect, and it is now the **ninth standing rule: a path in `LIVE_PATHS` and a handler that reapplies it are two different facts.** Three tests against a real runtime closed it (`TestTuningTheGateReachesTheRunningGate`), including one that asserts `companion_engine is None` produces `restart_required` rather than a lying `applied`. Round two: **28/28 caught, 0 survivors.** Known issue recorded rather than fixed: the two speaking gates keep independent budgets (proactive 4/day, companion up to 12/hour), so S20's "do not spam" is not enforced *across* both - merging them would silently change the owner's two configured numbers, which S2 forbids. `data/companion.json` and its `.tmp` sibling are gitignored for the same reason as the proactive ledger. |
| 15 | Proactive Agent | DONE | **An entire category had a branch, a cooldown, two templates and three passing tests, and could not fire in a real process.** S21's binding sentence is *"AURA must not silently perform arbitrary high-impact actions merely because it detected an event"*, so the phase had two jobs - make the proactive engine do what it claims, and establish that nothing on the bus crosses from noticing into acting. Both found a defect. `Category.APPRECIATION` was **unreachable in production**: `ProactiveEngine` has taken a `memories` callable since it was written and `launcher/services.py::_build_proactive` never passed one, so `engine.memories` was `None`, `_gather_memories()` returned `()`, and `if context.relevant_memories:` guarded dead code. Probed before editing rather than inferred (`'_build_proactive' passes memories=: False`). What let it survive is the **shape of its tests**: all three hand-built `ProactiveContext` with `relevant_memories=("...",)`, the one field production leaves empty - they exercised the behaviour correctly and constructed an input the composition root has no path to producing. That is a **third distinct variety of the S44 defect** (phase 11 read a copied default; phase 14 asserted a declaration; this one supplied unreachable state) and the **tenth standing rule: when a test hand-builds the input object, ask which field production fills and which it leaves empty** - a field that only ever has a value inside a test is a fixture describing a system that does not exist. `proactive/memories.py` (new, 131 lines) closes it: `EpisodicMemorySource` reads the **`project`** category - deliberately not `plan`, which `proactive/tasks.py` already owns for reminders, because mining one category for two kinds of unprompted message means saying the same thing twice in two voices - inside a window of older than 24h (repeating what somebody said an hour ago is not warmth) and newer than 14 days (past that it is bringing up something they have moved on from), subjects of at least 12 characters (the composer interpolates into "Been thinking about {subject}." and "ok" is not a subject), newest first, three at most, and hands back **the owner's own stored sentence** - nothing in the module writes, summarises or paraphrases. It is given `pipeline.episodic`, the same object the task source gets, with a test asserting that **identity** rather than that both are non-None, so an appreciation cannot be about a project recorded in a database the reminders never read. Red-verified: `memories = None` back in `_build_proactive` gives `2 failed, 2 passed` - exactly the two wiring tests, while the two that supply their own source still pass. **Second defect: an unlisted category had no throttle at all.** `proactive/decision.py` states in prose that "a category that is not listed here cannot be sent at all" - true of the decision engine, false of `ProactivePolicy`, which took a plain `str` and looked the cooldown up with `.get()`, skipping the whole per-category branch on a miss. Probed against the real policy with five distinct messages one second apart: `"task"` sent **1 of 5**, `"insight"` sent **5 of 5**. That is precisely the spam route the closed set exists to prevent, reachable from any caller that passes a string. Fixed with `KNOWN_CATEGORIES = frozenset(category.value for category in Category)` plus a refusal in `allows()`, placed after `enabled` and the empty-message check but **before quiet hours** so the reason an operator reads names the actual problem rather than the time of day. **Refuse, not a default cooldown**: inventing a plausible number for a category nobody declared would hide the caller bug and ship the spam (S44), where refusing makes the sentence `decision.py` already claims actually true. It does not restrict the owner (S2) - an unknown category cannot come from owner configuration, and every real category stays tunable. Derived from the enum rather than retyped, because a hand-maintained copy of a closed set is the thing that drifted in the first place; a drift-guard test now asserts every `Category` member has a `DEFAULT_CATEGORY_COOLDOWN` entry. Red-verified: removing the guard gives `2 failed, 2 passed`. **Seven tests had been leaning on that defect**, which is a large part of why it lasted: `TestTheProactiveLimitsSurviveARestart` used the category `"check_in"`, not a `Category` member and never was, and because an unlisted category had no per-category cooldown, using one left whichever rule each test was about as the only rule standing. Closing the set broke all seven; the repair names a real category (`Category.WELLBEING.value`) and stands the other rules down **explicitly** with `category_cooldown_seconds={CATEGORY: 0}`, because several of them discard the reason string with `allowed, _reason` and a test that asserts "not allowed" while a different rule than the named one does the refusing is a test that passes for the wrong reason. **Third finding, not a defect, and the one that needed a decision: nothing on the event bus acts on an event.** Enumerated at a fully built composition root with avatar, proactive and voice on and the outbox attached - `avatar.animation` (6 handlers), `events.log` (1), `avatar.state` (1), `avatar.controller` (1), `voice.tts.engine` (2), `server.notifications` (1) - every one renders, speaks, logs or queues, and none acts. But `subscribe` takes any callable and `publish` calls every match and swallows handler exceptions, so S21 held **by accident of what happens to subscribe**, not by construction. `TestNothingOnTheBusActsOnAnEvent` makes it a property: it walks **both** `_wildcard` and `_handlers` - `subscribe_all` stores into a separate list that `handler_count()` includes and a walk of `_handlers` alone misses, which is why the first probe printed 8 against a count of 9, and a boundary test that reads one structure is a boundary test with a hole in it - resolves each handler's owning module, and fails on any module outside a declared presentation set, with a message saying this needs an S21 decision rather than a list addition. A second test asserts the bus is **not vacuously clean**, since an empty bus would pass the first one and that is the phase 12 defect, not safety. **Verified to fire** against a synthesised `tools.autopilot` acting handler, which both tests caught. **A docstring that was wrong about which direction a value pushes.** `proactive/context.py::seconds_since_user` claimed every consuming rule uses the value "to *hold back*, never to fire" - the active-conversation guard does, but greeting, task and appreciation all fire on a **large** value and infinity is the largest. Probed: `last_user_message_at=None` yields `inf`, which yields `send: True, category: greeting, reason: user away, no record of a previous message`. So a brand-new owner with no message on record clears `GREETING_AWAY_SECONDS` on the first tick, and only `greeted_this_part` and the presence source stand between an empty history and an unprompted "welcome back". The docstring now names the firing path; the behaviour is recorded as a known issue rather than changed, because which one the owner wants is theirs to say (S2). Known issues carried forward: S21's "high-impact" question is now answerable but unanswered, and lands on phase 16 - the tool system is the one acting surface and no event can reach it today; and the two speaking gates still keep independent budgets (proactive 4/day, companion up to 12/hour), unchanged from phase 14 and still needing an owner decision rather than a silent merge. |
| 16 | Universal Tools | DONE | **S22 names seven things a tool exposes; six already existed and the two that did not had opposite answers.** `name`/`risk`/`execute` are `ToolProtocol`; `description`/`parameters` are read by `describe_tool`; S22's *permissions* is the existing five-gate system (gate 3 the allow-list, gate 4 `risk` + `auto_approve`). That left `capabilities` and `verify()`, and the phase's real work was deciding which of them earns a place. **`capabilities`: deliberately not added.** Nothing would read it - `risk` grades damage and drives approval, `tools.allowed` decides what may run at all, `parameters` declares the interface. A field with no consumer is the *wired, green and dead* defect, and a later reader would reasonably assume someone had measured it; the same discipline `recovery.py::RETRY_LIMITS = {}` documents. Recorded as a decision with its reasoning, not an omission, and it gets added when a phase brings a real reader. **`verify()`: added, because a live defect was already sitting in the gap.** S11 forbids resting on *"the command executed without throwing"*, and `_run` produces exactly that. `verify()` is **getattr-optional, never on the Protocol** - `test_the_protocol_stayed_narrow` fails the moment the Protocol widens, so the hook joins `timeout`/`describe`/`required_parameters` as optional-by-absence. The executor consults it after a **successful** `_run` and downgrades a success the postcondition denies, with three limits each pinned by a mutation: a failure is never re-verified (nothing to second-guess, and the side effect was never meant to hold), a `verify()` that **raises fails closed** (it yields precisely "it ran without throwing" for the verify, the one sentence S11 rules out), and `None` **asserts nothing** rather than failing. It is not a second verifier: `brain/recovery.py` reconciles device actions reported back over the wire against `CognitiveState`, a local tool never creates an `ActionRecord`, and the two never see each other's work. **The defect that justified it, proved before any code was written.** `remember` called `remember_user_stated` and returned `ok(f"remembered {key} = {value}")` while **discarding the return value** - which is the written `Belief`, or `None` when nothing was written. `normalise_key` keeps only `[a-z0-9]`, so a key that strips to an empty slug writes no row and raises nothing. Probed on the live tree: `key='???'` and `key='---'` both gave `ok=True`, `output='remembered ??? = Thien'`, **`rows=0`**. Then the case that makes it more than a curiosity - `key='名前'` gave `ok=True` and `rows=0` too, while `'tên'` and `'identity.tên'` stored fine. So a CJK key is silently lost and reported as saved, on the machine of an owner whose own filenames are Vietnamese, and the model is handed "remembered" to write into a reply telling the user a fact about themselves was kept. **Two layers, because they catch different things**: `execute` now fails on a `None` write with a reason naming the key so the model's next attempt can pick one that survives, and `verify()` reads the fact back through the same `value_of` the prompt recalls with - proving it is reachable the way it will actually be reached, which also covers a write that landed and was immediately invalidated. `open_application` deliberately gets **no** `verify()`: it already resolves the executable before spawning and watches the process through a grace period, and its postcondition cannot honestly be re-asked afterwards because the evidence is gone - absence means *execute already told the whole truth*, which is now written into the Protocol docstring so the next tool author does not read absence as an oversight. 16 tests, red-first (framework 4 failed / 4 passed - the 4 that passed did so because nothing consulted `verify()` yet; remember 7 failed / 1 passed). Five mutations, all caught: executor ignores the verdict (4 failures), `execute` discards the `None` return again i.e. the original defect, `verify()` passes by default instead of reading back, executor fails **open** when `verify` raises, executor re-verifies failures. |
| 17 | Accessibility integration | DONE | **Two defects, one shape: each was invisible because no test crossed the boundary the defect lived on.** **Defect 2 - a field declared, serialized, read by two server modules, and never assigned.** `AppInfo.activity` is read by `brain/agent_mode.py` (`context["app"]["activity"]`) and `brain/prompt_builder.py`, and the device never filled it, so every prompt described the foreground app with a package name and no screen. Fixed by tracking `TYPE_WINDOW_STATE_CHANGED` into a `lastWindow: AtomicReference<Window?>` and resolving it through `activityFor(lastWindow.get(), activePackage)`, which returns `null` - not `""` - when the cached window belongs to a different package, because `CognitiveState.observe()` reads `None` as *no news* and `""` as *nothing there* and the two are not interchangeable. **What let it survive is what the phase had to fix as well as the field**: nine device tests passed over this code and every one of them was blind to the serialized key, because none touched the serializer. Two wire-contract tests now encode `AppInfo` and `AccessibilitySnapshot` through `Json.encodeToJsonElement` and assert the key names - `packageName` carries `@SerialName("package")` and `activity` deliberately does not, so a rename on either side would leave the field permanently absent again. Mutation M4 renames the wire key to `"screen"` and fails **only** those two tests, which is the evidence that the other nine never covered it. **Defect 1 - two authorities on "done", and the device's was wrong in the dangerous direction.** The device's stop-early heuristics tested "more than one thing" only as *a conjunction*; the server's planner also decomposes *two verbs with no conjunction at all*. Proved before writing code, against the real planner: twelve concrete requests the planner splits into two nodes were classified single-step by the device - six reaching `shouldAutoComplete` (`mở YouTube tìm nhạc`, `open YouTube search for Minecraft`, ...) and six reaching `isSearchTaskComplete` (`search Minecraft then play`, ...). A false *stop* means the loop ends mid-task and the owner is told it succeeded. **The fix names an ownership boundary rather than merging the two systems**: the server owns *is the goal met* (`plan_for` -> `task_graph.build`; the model emits `complete`), the device owns only *may I stop without asking*, a latency optimisation permitted to be wrong only in the direction that costs a round trip. `shouldAutoComplete` now also reads `AuraActionExecutor.SEARCH_VERBS`; `isSearchTaskComplete` defers when `hasClauseAfterSearch` finds work named after the search. **Positional, measured from the last search verb, not a containment test** - a conjunction *before* the search is the ordinary two-clause request ("open YouTube and search Minecraft") which must keep ending at the submit; M9 (first verb instead of last) is caught. Widening `selectionCues` was rejected instead: three of the planner's six trailing verbs (`tap`, `click`, `bấm`) are not in the device's list at all, and adding bare `open`/`mở` would be misread there as a launch verb. **One vocabulary per concept.** `SEARCH_VERBS` was promoted from a local `val` inside `sanitizeSearchQuery` to a companion constant with three readers; `selectionCues` had been declared **twice, identically**, in two functions and is now one. The Python invariants extract these lists **out of the Kotlin source** rather than keeping a copy, because a Python copy agrees with itself forever while the device drifts - which is exactly how `submit` came to be offered by the prompt and rejected by the parser. **Vacuity was the real risk and is closed structurally**: an assertion over a declared list passes trivially if nothing reads the list, which *is* the pre-fix state, so the tests also read the **bodies** of `shouldAutoComplete` and `isSearchTaskComplete` and fail if the reference disappears - M6 and M8 prove those guards fire. `test_the_device_conjunctions_cover_the_planners` asserts **containment, not equality**, in the one direction that matters. Two false comments were corrected in `brain/planner.py` (it claimed `CONJUNCTIONS` was "deliberately the same set" as `multiStepKeywords` - extracted both from source, it is not) and one stale note in `tests/test_planner.py`. **Deliberately not done, and recorded rather than smuggled in**: there is still no device-to-server "task ended" signal (`clear_task()` has no production caller; the only channel is `repository.send(AGENT_TICK, ...)`, a full model turn, so this needs a non-LLM endpoint - a wire change on both sides), and the launch-only verification restriction in `brain/task_graph.py` / `brain/recovery.py` is now **unjustified** since both justify it with "the device never fills in the activity name" and it does now - S11 work, and doing it in the same change that made the field real would make a regression in either indistinguishable. |
| 18 | Windows Agent | IMPLEMENTED | **18.1 observation + window management: IMPLEMENTED. 18.2 controlled command execution: IMPLEMENTED. 18.3 filesystem writes: IMPLEMENTED. 18.4 screenshots: IMPLEMENTED. 18.5 input synthesis (keyboard, mouse): IMPLEMENTED.** **18.5** is four DANGEROUS tools in `tools/builtins/input.py` - `move_mouse`, `click_mouse`, `type_text`, `press_keys` - behind `SendInput`, registered by `_pc_tools()` beside the 18.1 window tools and sharing their one window source. This is the most dangerous thing in the PC layer, and structurally rather than rhetorically: every earlier phase either read the machine (18.1, 18.4) or wrote somewhere the owner had explicitly named (18.2's declared commands, 18.3's `writable_paths`), and this one presses real keys into whatever window happens to be in front, with no list of permitted targets to check against because there is no such list to write - the target is "the desktop". **Two measurements taken before any code was written.** `SetCursorPos(2420, 1580)` on this 1920x1080 desktop **returned true and left the pointer at (1919, 1079)**; `(-40, -40)` returned true and landed at `(0, 0)`; in-bounds moves are exact. Section 11's sentence is therefore the literal failure mode of the primary API here - a click aimed off screen lands on whatever occupies the corner and the caller is told it worked - which is why `_target` refuses off-desktop coordinates **before** moving and `verify` re-reads the cursor afterwards past a 2 px tolerance (measured exact, slack for a coarser host, not enough to hide a clamp). And a wrong `cbSize` is completely silent: `SendInput(1, array, 8)` returned 0 with `GetLastError()` at **0**, so the accepted-event count is the only signal that entire family of failure has, which is why `_send` returns it and `_sent` compares it against what was submitted. `sizeof(INPUT)` measured **40** here rather than being assumed. **The `window` guard is the real safeguard, not a fourth risk rung.** A plan two steps long - focus the editor, then type - has a gap the tool cannot see: between the steps the owner alt-tabs to their bank, and step two types into whatever is there now. `window` turns silent misdelivery into a refusal, and **a guard that cannot be checked is refused, not skipped** - no window source, an unreadable desktop, or nothing reporting itself active all raise, because ignoring a safeguard when the desktop is unreadable would hand the caller the words without the protection. All four stay DANGEROUS, the **top of the existing ladder** rather than a new rung, and `ToolRisk.DANGEROUS`'s own docstring already names "click" as its example: a fourth level would have to be learned by the settings contract, the Android DTO, `config.yaml` and the Hub before it protected anything, and it protects nothing the owner cannot already get by leaving `dangerous` out of `tools.auto_approve`. `click_mouse` requires x and y rather than clicking wherever the pointer happens to sit, so the confirmation prompt names where the click lands instead of an earlier call having decided that. **The guard justified itself before it was ever tested**: the end-to-end probe meant to drive Notepad found **`cf-backup-codes.txt - Notepad` already open** - the owner's own file, on the machine the suite runs on - refused, and Notepad was abandoned entirely. **Each postcondition names what it cannot see.** `press_keys` asserts that no modifier it pressed is still held - `GetAsyncKeyState` was measured seeing a *synthesized* VK_SHIFT, so that is a real read and not a decorative one, and the failure it catches is a CTRL that never came back up turning everything the owner types next into shortcuts - and it claims nothing about `ctrl+s` having saved. `click_mouse` delegates to the pointer check and claims nothing about the click having *done* anything, because only the application knows whether the button under the pointer was enabled. `type_text` re-checks only that the named window is still in front and its docstring says exactly that. `verify` returning `None` where the desktop is unreadable is deliberate: the executor reads None as no postcondition offered, which is honest - nothing was learned - and is not the same as the action having failed. One limit cannot be closed: Microsoft documents that input blocked by UIPI is reported through **neither** the return value **nor** `GetLastError`, so a full accepted count does not prove arrival at an elevated window, and `_sent`'s message names that possibility rather than claiming delivery. **The suite reads the real machine and never acts on it** - `TestTheWindowsBackend` presses no key and no mouse button, because the owner may be using this machine while it runs, with the one pointer move it does make restored in a `finally`. Correctness was established against an independent oracle rather than a mock: a tkinter window we own, driven by the production tools, read back through the widget's own contents rather than a clipboard copy that could fail on its own - six oracles, **FAILURES: none**, including `shift+q` producing `Q`, which is what proves the release ordering. Two defects the writing itself found (the mock's accepted count cancelled itself by operator precedence, so every count test would have agreed with itself for the wrong reason; `split_typing` collapsed a blank line, turning two paragraphs into one) and three the **45-mutation battery** found are in `progress.md`; the third is worth naming here, because the backend's event stream had **no unit coverage at all** and it holds the worst failure in the file - releasing CTRL before the letter turns `ctrl+s` into a stray `s` typed into the document, which hardware caught and the suite did not. `TestTheEventsSentToWindows` now asserts the order, the KEYUP flags, the extended flag on *both* surrogate halves and the UTF-16 pair without a keyboard. **Nothing is enabled**: the shipped `config.yaml` registers 9 tools including these four, `tools.allowed` is still `['current_time', 'remember']` and `auto_approve` is SAFE only, so gate three is what stands between a registered tool and a running one. No new config keys, so no Android fixture change and nothing for the settings contract to learn. Owed and recorded: Windows' foreground lock is **still** unexercised on hardware, carried unchanged from 18.1 because the Tk window already held the foreground; UIPI blocking of synthesized input into an elevated window cannot be exercised without running something as administrator; and no Hub widget shows that the four are available (phase 23, joining `take_screenshot`, `tools.commands` and `tools.writable_paths` as read-only display debt). **18.4** is one DANGEROUS tool, `take_screenshot` in `tools/builtins/screen.py`, and two things it turned out to need. The brief assumed `vision.capture.ScreenshotCapture` could be reused; it wraps `mss`, which is optional and **not installed here**, so `capture_screen: true` had been quietly doing nothing on the owner's machine - a latent defect phase 18 walked into rather than caused. Instead of adding a dependency (section 41), `GdiScreenCapture` was written beside it using the ctypes route 18.1 already uses for `user32`: `GetDC` to `CreateCompatibleDC` to `CreateCompatibleBitmap` to `BitBlt` to `GetDIBits`. `encode_png` writes the file with `zlib` and `struct`, both stdlib, so the whole PC layer stays installable by doing nothing. `default_screen_capture()` prefers `mss`, falls back to GDI, and returns **None** rather than a `MockScreenCapture` - a mock wired in by default would let the tool report success having written a blank image. `launcher/services.py` now builds vision through it, which is why pixel capture works on this host for the first time. **Section 11 is the whole story of this phase.** `GetDIBits` filling `width*height*3` bytes is exactly the sentence section 11 forbids resting on: a vertically mirrored image has the identical length, and so does one with red and blue swapped, and both are live risks here - GDI writes bottom-up rows for a positive `biHeight` and hands back BGRX not RGB. So the pixels are compared against PIL's `ImageGrab`, an independent implementation of the same capture, and against that reference deliberately flipped and channel-swapped, which must match worse: **2400 of 2400 sampled pixels identical, worst channel delta 0**, versus 90.3 percent for the flipped reference and 4.9 percent for the swapped one. That is a test, and it is the only test that catches the `biHeight` mutation. `verify()` reads the file back and parses the signature and IHDR, and **refuses to claim the picture shows the screen** - the screen has already changed and a second capture is a different moment; the docstring says so rather than leaving the gap for someone to fill in later. **Nothing is captured on a path that will be refused**: `execute` proves containment, suffix, overwrite and parent before reading a pixel, five tests assert a counting fake capture's `captures == 0`, and moving the proof after `capture.capture()` is caught by all five. No new grant and no new setting: the destination is `tools.writable_paths` through 18.3's own `_contained`, `_atomic_write` and `_shown`, **imported** rather than reimplemented because two containment checks can drift and the one that drifts is a path escape. Registration is inside `if writable:` and gated again on `default_screen_capture() is not None`, so a headless machine gets the writers and no screenshot. Shipped config registers nothing. The display index is the owner's: `monitor: "2"` is read, `monitor: yes` is refused, and an index no display answers to falls back to the primary with one warning rather than to index 0, which is every monitor stitched into one wide image. The tool takes a **factory** rather than a capture object because the shared alternative would suppress that warning for a second bad index. DPI awareness is thread-scoped and restored in a `finally` - the process-wide call is permanent and not a screenshot's decision to make - and its benefit is **unverifiable on this single-display 100-percent-scaled host**, which is recorded rather than claimed. Owed and recorded: `OllamaVisionProcessor._to_png` should fall back to `encode_png` when PIL is absent (left alone per section 41); `_monitors()`'s primary-first sort is unobservable with one display; no Hub widget shows that `take_screenshot` is available or which root it writes to. Four tools, no new abstraction - each is a `ToolProtocol` with an optional `verify()`, behind the five gates phase 16 built. `system_information` and `list_processes` (both SENSITIVE) in `tools/builtins/system.py`; `list_windows` (SENSITIVE) and `focus_window` (DANGEROUS) in `tools/builtins/desktop.py`; joined by a new `tools/factory.py::_pc_tools()`. **Registered, not enabled** - no new config keys, `tools.allowed` is still `['current_time', 'remember']`, and a test reads `config.yaml` directly so a loader defaulting bug cannot hide a silent enable (section 2 cuts both ways: the owner must be able to enable these, and must not find them already enabled). **Section 11 is the substance of the phase**: `SetForegroundWindow` accepts-and-ignores under Windows' foreground lock, so `execute` says "asked for" and `verify` reads the foreground window back on a bounded 1.5s poll; a live probe showed `execute` ok=True with `verify` failing 1.50s later, and an executor-level test proves the framework downgrades it. The three readers have **no** `verify()` on purpose - a read's postcondition is its return value. Both `default_*_source()` return `None` so the factory's absent-dependency rule applies; `list_windows`/`focus_window` register together sharing one source object. Two live-machine defects fixed (two windows titled exactly `Settings` made the ambiguity advice impossible and the failure message nonsense - optional `pid` threaded through `_match`/`execute`/`verify`). One false claim of mine deleted per section 44: undeclared `GetForegroundWindow` does **not** truncate here (203 windows, largest handle `0x1202A0`); the real instance is `GetTickCount64`, negative after 596.5h uptime with this machine at 300.7h - both now structural guard tests. **Not exercised:** the real cross-window switch under the foreground lock (owner's foreground was a fullscreen game). **18.2** is one tool, `run_command` (DANGEROUS), in `tools/builtins/commands.py` (~1090 lines), and its shape is section 24's sentence read literally: the model never composes a command line, the **owner** declares named argv lists in a new `tools.commands` section, and the model supplies the key plus values for declared slots - one slot per argv element, `shell=False`, no interpreter in between. Two declaration shapes (bare argv list, or a mapping with `argv`/`description`/`parameters`/`cwd`/`timeout`) because the short one is what an owner types and the long one is what they need once they want a working directory. **The property is demonstrated, not asserted about a mock**: fifteen shell-meaningful payloads (`a && b`, `$(echo hi)`, backticks, `%PATH%`, a literal quote, a literal newline, `--looks-like-a-flag`) each come back from a real subprocess as exactly one `sys.argv` entry, and `["--flag", "{value}", "--after"]` filled with `x --injected y` arrives as three elements, not five. **The batch-file claim inherited from the 18.2 brief was re-measured and found too broad** - `& && \| ^ >` are neutralised on Python 3.11.15 (the CVE-2024-1874 fix is present); the live condition is a literal `"`, which breaks out and runs the remainder as commands, plus `%CD%`/`%PATH:~0,12%` expanding. So the refusal is scoped: a resolved `.bat`/`.cmd` **with a fillable slot** is refused before anything spawns, one without a slot runs (there is no model-supplied text for cmd.exe to re-parse, and refusing the owner's own argv would be the arbitrary restriction section 2 forbids). Not exotic: `shutil.which` resolves `npm`, `npx` and `code` to `.CMD` shims here. **A defect in my own code, found by my own test**: a leftover-brace check refused any argv still containing `{...}` after substitution, which breaks `find . -exec rm {} \;`, `grep -E "a{2,3}"` and `jq "{name: .n}"` - and the justification was wrong, because those braces are the *owner's* text and section 24 fences *model-supplied* text. Narrowed to `NEAR_SLOT` (matches only `{ pattern }`, `{my-pattern}`), execute-time refusal deleted, load-time warning kept: warn, do not override. **A section 30 path nobody asked about, closed**: `core/credentials.py` puts stored keys into `os.environ` and children inherit it, so `run_command` with any environment-dumping program would have put the owner's key in chat history. `_child_environment` strips `brain/router.py::PROVIDER_KEYS` and `core/credentials.py::SECRET_ENV_VARS`, imported **at call time** so a provider added in phase 19 is covered with no edit here, plus a `KEY\|TOKEN\|SECRET\|PASSWORD\|PASSWD\|CREDENTIAL\|AUTH` sweep, with one documented exception (`SSH_AUTH_SOCK` is the path of a socket, not its contents, and `git` over SSH needs it). The test asks the child itself whether anything credential-shaped arrived and whether a known value arrived under some *other* name. **Bounds nest outwards**: `self.timeout = longest_declared + KILL_GRACE + 5.0`, because the executor bounds `execute` on a daemon thread it cannot kill and an inner bound would abandon that thread mid-kill; `timeout: 0` stays unbounded rather than being clamped, since `tools/timeout.py` documents 0 as the no-bound hatch and clamping the owner's explicit 0 is the silent mutation section 2 forbids - warned instead. **Temp files, not pipes**, re-measured: a batch file leaving a `ping` grandchild returned after **29.25 s** under `subprocess.run(timeout=1.0)` (the grandchild held the inherited stdout pipe and `taskkill /F /T` did not unblock the read); temp files + `wait(timeout)` + `kill()` + `taskkill /F /T /PID` returned in **1.08 s** with output captured. The Windows test asserts under 10 s then reads `tasklist` for no surviving `PING.EXE`, because the process table is the only honest answer to "did the tree die"; an earlier version of the portable test claimed to check that and only re-timed, so it was replaced. **No `verify()`, deliberately**: the exit status *is* the postcondition read back from the world, not "executed without throwing" - re-asking would re-run the command for double the side effects and no new information, and a test asserts the absence is intentional so nobody fills in the perceived gap. **Nothing is enabled**: `config.yaml` ships `commands: {}`, `allowed` is still `['current_time', 'remember']`, and four tests read `config.yaml` **from disk** so a loader defaulting bug cannot hide a silent enable. `tools.commands` is deliberately **absent** from `core/settings_store.py::ALLOWED` - a settable one would let anything holding the bearer token declare `["cmd", "/c", "{x}"]` and then fill `{x}`, i.e. arbitrary shell execution reached *around* the tool boundary through the settings API; guarded on both sides (a Python test on `ALLOWED`, a Kotlin test that `configurable` carries no `tools.commands` prefix). **Device side**: `core/config.py`'s new default surfaced `effective.tools.commands` and failed `test_settings_fixture.py`; fixed by a one-line targeted edit to `live/settings.json` rather than regenerating, because `AURA_WRITE_ANDROID_FIXTURES=1` must never run on this host and section 45 asks for targeted edits anyway. `ToolsConfigDto` gained `commands: Map<String, JsonElement>` - raw JSON, because parsing two server-owned shapes here would duplicate a schema the PC owns and fail closed on a shape a newer server added. Three device-side section 2 guards, including that the older hand-written body without `commands` must still parse. **Three mutation survivors, three real gaps, none an equivalent mutant**: (1) "a command line string is split instead of refused" survived because the generic not-a-list branch also refuses it - the value was in the *wording*, since `applications` accepts exactly that shape, so the message is now asserted; (2) "credential scrub keeps Aura's own names" survived because every current name in `PROVIDER_KEYS`/`SECRET_ENV_VARS` contains `KEY` or `TOKEN` and the pattern sweep catches it anyway, so the imports' only real value - *future* names - was verified by nothing; fixed by monkeypatching in `ACME_SESAME`, which no pattern matches; (3) "run_command registered with no declarations" survived because `RunCommandTool({}).available` is empty and the inner check blocks registration either way - what the outer `if commands:` gate actually decides is whether a stock server logs "none of the 0 declared command(s) could be used" on every startup, a warning about a problem the owner does not have in a log where a real one must stand out; both the test and a code comment now say so, so it is not deleted as redundant. Owed and recorded: no Hub widget renders `tools.commands` (phase 23; read-only display debt, distinct from the fourteen settable-but-unrendered paths), and the quote-breakout measurement is specific to CPython 3.11.15 on this machine - the refusal covers the whole class, but the *reason in the source* is a measurement a future CPython could shift. **18.3** is four DANGEROUS tools in the module that already held the readers, `tools/builtins/filesystem.py` (137 -> 688 lines): `write_file`, `append_to_file`, `create_directory`, `delete_file`, all four with a real `verify()` - the first place in the PC layer where the postcondition is both readable and cheap, which is the point of comparison for the rest (`focus_window` can read its condition back but the answer decays, so it polls; `run_command` cannot re-ask at all, because the exit status *is* the postcondition and asking again means running the command twice). **The decision the phase turns on: a reading grant is not a writing grant.** `tools.writable_paths` is a second list, read separately in `tools/factory.py`, and deliberately **not** defaulted from `tools.allowed_paths` - an owner who listed a folder so Aura could look something up did not thereby say she may overwrite it, and inheriting the read roots would be the already-made decision section 2 forbids. Tested from both ends: `allowed_paths` alone registers no writer, `writable_paths` alone registers no reader, and with two different folders each tool refuses the other's - with the file's bytes checked afterwards, not just the exception. **A measurement that changed the code**: `_atomic_write` creates its temporary with `tempfile.mkstemp(dir=str(target.parent))`, and the `dir=` is load-bearing rather than tidy - `os.replace` across volumes fails on Windows with ERROR_NOT_SAME_DEVICE because CPython calls `MoveFileExW` without `MOVEFILE_COPY_ALLOWED`; measured here (`%TEMP%` on C:, repository on D:), so a temporary in the system temp directory would have made every write to a folder on any other drive fail. **No test could have seen it**, because pytest puts `tmp_path` under `%TEMP%` so the same-volume case always holds there; the mutation battery surfaced it, not the 117 passing tests. Now covered by a cross-volume test that looks for a second drive and skips when there is not one, plus a structural assertion that always runs. `mkstemp` rather than the repository's usual `<path>.tmp` for a second reason: the path came from a model, so `notes.md.tmp` may be a file the owner already has. **The escape that could not be tested, and the one that could**: the symlink-containment test skips here with `WinError 1314, A required privilege is not held by the client`, which would have left the single most important property in the phase permanently unexercised on this host - so the same escape is exercised by a **directory junction**, which needs no privilege and which `resolve()` sees through equally (verified with `mklink /J` before relying on it). The junction is removed inside the test's own `finally`, because `shutil.rmtree` follows one into its target and pytest reaps old `tmp_path` directories with `rmtree`. A **hard** link is not caught and cannot be - both names are equally real - recorded rather than papered over, and not reachable through Aura since no tool creates links. **Two defects in my own tests, found by mutating my own code** (22 mutations, 20 caught first pass, both survivors real gaps rather than equivalent mutants): (1) the temp-collision test planted its decoy at `notes.tmp` while the repository's `with_suffix(suffix + ".tmp")` idiom produces `notes.md.tmp`, so it named a file no plausible implementation would touch and could never fail - it now plants both spellings; (2) all three Kotlin guards I added would have passed with a misspelled `@SerialName`, because `writablePaths` defaults to an empty list and every body asserted against was empty, so empty-because-absent and empty-because-defaulted were indistinguishable - the inline body now carries a real writable root and the test reads the value back, which also closed the identical pre-existing gap for `allowed_paths`, and load-bearing was confirmed by misspelling the `@SerialName` and watching the suite go red. **Each refusal is for a specific model failure**: an existing file needs `overwrite=true` (a model that means `notes-2026.md` and types `notes.md` would silently destroy a year of writing), with the message naming *both* ways forward or it gets retried verbatim; `append_to_file` refuses a missing file rather than creating it the way shell `>>` would (a mistyped target becomes a new file, reports success, and leaves the owner's real log empty while Aura reports writing to it daily); `delete_file` never takes a directory (different blast radius, nothing to read back afterwards to learn what was lost); `create_directory` creates parents while `write_file` does not, which is not an inconsistency - an extra empty directory is obvious and costs nothing, an extra *file* down a mistyped path is a stray copy of real content; oversize content is refused rather than truncated, because truncation is silent data loss that would pass `verify` only if `verify` truncated identically, and `append_to_file` bounds the resulting total or a bounded append repeated is unbounded. Newlines are **not** translated (`Path.write_text` would turn every `\n` into `\r\n` on Windows, so the file would not contain what was asked for and the byte comparison in `verify` could never pass). **Section 30's habit one level out**: every message here ends up in a prompt and so leaves the machine, and an absolute path names the owner's home directory and username - the same reason `system_information` reports the disk and not the user - so `_shown` renders root-relative POSIX paths, asserted structurally against `str(Path.home())` in both success and refusal messages. Two smoke-test defects fixed: `_contained` resolving a bare filename against Aura's working directory now gets its own message naming the actual problem (containment semantics deliberately unchanged - with multiple roots there is no non-arbitrary root to resolve against - and confirmed by grep that no test pinned either string), and `create_directory` reported `created c` for `a/b/c`, losing the structure the tool exists to create. **Nothing is enabled**: `config.yaml` ships `writable_paths: []`, none of the four names is in `allowed`, `auto_approve` is still `['safe']` (so in server mode with no confirmation handler a DANGEROUS call is refused before it reaches the tool, and there is a test for that state because it is the shipped state), and the guards read `config.yaml` **from disk** so a loader defaulting bug cannot hide a silent enable. `tools.writable_paths` is deliberately absent from `ALLOWED` for the same reason `tools.commands` is - a settable one would let anything holding the bearer token add `C:/` and have `write_file` replace whatever it liked, i.e. filesystem access reached *around* the tool boundary through the settings API - guarded on both sides. **Device side**: the new default surfaced `effective.tools.writable_paths` and failed `test_settings_fixture.py` exactly as `commands` did, predicted in the brief before it happened; fixed by a one-line targeted edit to `live/settings.json`, never a regeneration, because `AURA_WRITE_ANDROID_FIXTURES=1` rewrites fixtures from this host's live state and there is a real Gemini key in `.env` - the section 30 violation that already happened once in phase 10. `ToolsConfigDto` gained `writablePaths`; not forced (`ignoreUnknownKeys = true` meant the device tolerated the key), added because a writable root is arguably the most important line in the tools config for an owner to *see* from their phone. Section 24 items still owed (18.4): keyboard input, mouse interaction, screenshots - and the 18.3 brief's assumption that screenshots need `mss` is **wrong** and corrected in `current-task.md`: ctypes + GDI `BitBlt` needs no third-party dependency, the same way 18.1's window enumeration uses ctypes `user32`. Owed and recorded: no Hub widget renders `tools.writable_paths` (phase 23, joining `tools.commands` as read-only display debt); `append_to_file`'s `verify` proves the added text arrived and **not** that nothing else changed, because the prior length is not among the arguments `verify` receives and stashing it on the instance would be a lie the moment two appends overlap - said plainly in the docstring; and the cross-volume test skips on a single-volume host, where the weaker structural assertion is what runs. |
| 19 | Vision | IMPLEMENTED | **19.1 the processor chain: IMPLEMENTED. 19.2 the on-demand tool: IMPLEMENTED. 19.3 verification: IMPLEMENTED.** 19.1 fixed an inversion rather than adding a feature: turning `vision.capture_screen` on made vision report *less*, because the pixel processor *replaced* `WindowTitleProcessor` instead of layering over it, and `VisionManager.refresh` reads an empty description as "no observation". `vision/processor.py` `ProcessorChain` asks each processor in turn and takes the first non-empty answer, advancing on **both** ways a processor declines - `OllamaVisionProcessor` returns `""` for every failure it has, `CloudVisionProcessor` raises `ProviderUnavailableError` - so a dead Ollama daemon degrades to the window title instead of going silent. Deliberately not `FallbackProvider` (that would make `vision/` import `brain/providers/`) and deliberately not a concatenation (the pixel model can read the title out of the pixels). `CloudVisionProcessor._compact` now encodes raw RGB from a desktop backend as well as pre-encoded phone frames, and the prompt names the right screen from `Frame.source`. Three new settable paths: `vision.capture_screen`, `vision.send_screen_to_cloud`, `vision.min_interval`, with the Android `VisionConfigDto` chain updated by hand. 19.2 added the on-demand half: `tools/builtins/vision.py` `DescribeScreenTool`, one more tool through the same five gates. `execute` calls `refresh()` not `get_context()` (the throttle that stops fifty turns becoming fifty screenshots would answer a question nobody asked) and refuses when `vision.enabled` is off - section 2, because `refresh()` itself does not consult the flag, so its absence here would be a tool looking at a screen the owner closed. Its risk is read off the processor chain **per instance** via a new `sends_pixels_offsite` flag: SENSITIVE while pixels stay on the machine, DANGEROUS once a hosted model is in the chain, because section 30 does not let "send a picture of the screen to a third party" ride on "read the screen"'s permission. `verify` reads new `VisionManager.last_observation` / `.seconds_since_observation` properties rather than `get_context()`, which re-observes when its throttle expires - with `min_interval: 0`, every call - so verification would have paid for a second capture and, with a cloud link, a second upload. Freshness is the load-bearing half: "I looked and saw X" and "I did not look and X is what I remember" are the same string, and section 11 forbids trusting the second. Registered is not enabled twice over - `tools/factory.py` registers it only while `vision.is_available()`, and the shipped `config.yaml` `tools.allowed` still names `current_time` and `remember` only. Debt to phase 23: no Hub widget for the three new vision paths or for `describe_screen`'s availability; the `server.screen.min_interval` vs `vision.min_interval` conflation over one live attribute is recorded, not resolved. |
| 20 | Voice | PARTIALLY IMPLEMENTED | **Audit-only phase; owner deferred the remainder.** Push-to-talk STT and event-driven TTS are IMPLEMENTED and wired: `launcher/services.py::_build_tts`/`_build_stt` (both gated on `voice.*.enabled`, default off in shipped `config.yaml`), CLI `/voice` push-to-talk through `runtime.listen()` → `chat(source="voice")`, `TTSEngine` subscribed to `ResponseEvent`/`MoodChangedEvent`, providers Edge/SAPI/pyttsx3/mock with factory fallback, `tools.allowed`-style two-lock settings contract (`voice.tts.enabled/provider/voice/volume/playback`, `voice.stt.enabled` in `core/settings_store.py`) with Android `VoiceSection.kt`. 127 tests green. **Deferred by owner:** `StreamingSpeaker` is complete and tested but has zero production callers (streamed replies are spoken whole after generation); the cancellation chain (`EdgeTTSProvider.cancel` → `SystemAudioPlayer.stop` → `TTSEngine.stop`) is correct and tested but no production path invokes it (no barge-in/interruption); `listen_continuous` + `KeywordWakeWord` exist and are configured (`voice.stt.wake_word`) but no host ever enters the loop; hosted-TTS offsite disclosure (the sends_pixels_offsite-shaped question for audio - Edge sends reply text to Microsoft) is unanswered. None of it blocks phases 21-25. |
| 21 | Plugins / Skills | IMPLEMENTED | **Found implemented per ROADMAP section 8; audit confirmed every requirement on real execution paths; closed one test gap.** Discovery (`plugins/discovery.py`: bundled package + `plugins.directory`, three advertisement forms INSTANCE/factory/subclass-scan, `aura_plugin_` import prefix so a user file cannot shadow stdlib, `_`-prefixed modules skipped), registration with shape validation and duplicate rejection, lifecycle initialize→shutdown in reverse order, enablement policy separate from discovery (`plugins.enabled`: list = exactly those, true = all, empty/missing = none - a deliberate "none" distinct from unconfigured), failure isolation (import/init/shutdown errors logged and skipped, never fatal), capability boundary (plugin receives bus + tool *registry*, never the executor; its tool still needs `tools.allowed` - enforced at `ToolExecutor.available()` which filters registry names by policy). Production wiring verified: `_build_plugins` runs last in `build_services`; shutdown first in both runtimes' `stop()`. Config keys `plugins.enabled/directory/config` with per-plugin config narrowing. 42 component tests existed; the missing piece was the composition root - the exact phase 11 blind spot where dropping `_build_plugins` from `build_services` would have left the suite green. Added three tests in `tests/test_plugins.py` driving the real `build_services` against an on-disk plugin: plugin initialized, its tool registered into the process's actual registry, runnable through the executor when allowed, registered-but-not-offered when not named in `tools.allowed`, and `Services.plugins is None` when disabled. Plugin tests now 45; full suite `3116 passed, 3 skipped, 1 deselected`, 0 regressions. |
| 22 | Diagnostics | IMPLEMENTED | **Found largely implemented; closed one real gap - plugin/tool status was absent from every diagnostic surface.** Already in place and verified on real paths: `/api/health` liveness + a `runtime` map of actual state (`llm_provider` reports the *built* chain - `"unavailable (ErrorType)"` when construction raises, not a cosmetic "healthy"; memory/vision/voice/screen/companion/proactive each read their live object); `/api/ready` true readiness returning 503 with failure reasons, deliberately excluding optional collaborators so an off TTS cannot fail a probe; `/api/providers` + `/api/providers/health` per-provider status with no secrets; `/api/settings` effective-document + `restart_required` semantics; `events/log.py` event log attached unconditionally at build; startup/shutdown summary logging in both runtimes; Android `DiagnosticsSection.kt` renders `runtime.entries` generically plus per-provider rows. **The gap:** `PluginManager.status()`/`summary()` were built for diagnostics ("useful for diagnostics and for a settings UI", said its own docstring) and had no production caller except a startup log line - a broken plugin was invisible everywhere outside tests. Fixed in `server/runtime.py::health_status`: new `tools` key counts what the executor would *actually approve* (registry ∩ `tools.allowed`, not registry size) and new `plugins` key is the manager summary with failed plugin names spelled out (`"1/3 plugins active (bad_plug failed to initialize)"`). Wire-compatible: `HealthDto.runtime` is `Map<String, String>`, keys render automatically in the Hub. Contract extended deliberately, following the phase 16 pin precedent: `test_health_exposes_only_the_documented_keys` updated with reasoning in its comment, docs/API.md example + prose updated. New tests: tools-disabled/plugins-off shape, allowed-filtered tool count, broken-plugin naming against a real `ServerRuntime`. Focused suites `215 passed, 1 skipped`; full suite green. |
| 23 | Settings / UI integration | IMPLEMENTED | **The phase's own recorded debt was the work: 14 setting paths were settable over PATCH with no Hub control anywhere.** Re-audited the whole pipeline (`core/settings_store.py` ALLOWED table → `server/settings_service.py` PATCH/`restart_required` → Android DTOs → Hub sections) and diffed every configurable path against literal *and* dynamic widget bindings - ModelsSection binds per-provider model keys through `ProviderDto.modelSetting`, so those were never gaps. The real list: 6 × `server.companion.*` tuning knobs (only `enabled` had a row), 3 × vision paths from phase 19 (`capture_screen`, `min_interval`, `send_screen_to_cloud`), `temporal.timezone`, 5 × `llm.task_models.*`, `llm.custom_base_url`, `llm.custom_model`. Closed all of them: `CompanionConfigDto` extended with the five knobs + new `TemporalConfigDto` on `EffectiveConfigDto` (all defaulted, so an older server still parses); NotificationsSection gained a "Tuning" block (relevance slider, cooldown/hourly-cap/suppress/repeat-window steppers, quiet-hours editor with the server's own bounds restated in subtitles - nothing can turn the anti-spam gate off, because nothing server-side can either); VisionSection gained "What Aura reads" (capture toggle + interval stepper) and a Privacy block where `send_screen_to_cloud` is worded as the permission question it is; GeneralSection gained the timezone editor; ModelsSection gained Task routing (five lanes as data, blank = same-as-primary) and Custom endpoint rows. `QuietHoursDialog` extracted from ProactiveSection to serve both gates rather than being cloned; `HubViewModel.setQuietHours` path-parameterized. Capability-granting paths (`tools.allowed/allowed_paths/writable_paths/applications/commands`) stay deliberately widget-less - a bearer token changes settings, not what tools may touch; that boundary and its section 24 rationale are already documented in the allow-list itself. Contract tests: backend settings suite `191 passed, 1 skipped`; Android unit tests **`378 passed, 0 failures`**. `describe_screen` availability is owner-visible through ToolsSection's allowed-list row plus phase 22's live tools count in diagnostics. |
| 24 | Performance optimization | IMPLEMENTED | **Baseline first, one measured fix, no speculative refactors.** Baseline: `load_config()` = ~10.5 ms/call, of which `yaml.safe_load` = 6.8 ms - and it runs several times *per chat turn* (`ChatEngine._history_limit`, `_personality_section` ×3, plus provider/vision constructors), so every turn re-parsed an unchanged file. Fix: a merge cache in `core/config.py` - the three-layer merged config is kept and rebuilt only when something could have moved: config.yaml's stat; the runtime overlay's new per-store random token (`RuntimeSettings.cache_token`, not `id()` - CPython may hand a freed store's id to its replacement, which would serve one store's merge for another); or the overlay's new version counter (`version`, bumped only after a successful persist in `update()`/`reset()`, so a failed save never poisons the key). Callers get a fresh `deepcopy` (0.055 ms measured) - the old fresh-dict contract preserved exactly. After: **0.077 ms/call (~136×)**. Five regression tests pin the rules (no-reparse proof via counting `safe_load`; rewritten-file visibility; copy independence; overlay update/reset visible without touching config.yaml; fresh-store swap invalidates even at equal version 0). Examined and deliberately left alone: `build_styler` per turn 0.14 ms; `build_services` ~300 ms once at boot; SQLite writes are required persistence; no tight polling loops in `proactive`/`server`/`vision`/`launcher`. Full suite after: **3124 passed, 3 skipped, 1 deselected**. |
| 25 | Full integration / RC | **RELEASE CANDIDATE — READY** | **Fresh repo-wide audit, not an inheritance of the per-phase marks.** Architecture: plugins load through the composition root (`launcher/services.py::_build_plugins` → `Services.plugins`), shut down in server stop order (`server/runtime.py` plugins-first), and their failures are named, not counted, in diagnostics (`_plugins_label` → `PluginManager.summary()`); no orphaned required components, no duplicate competing implementations. Configuration: the phase 24 cache was re-audited under RC rules - invalidation keys on file stat + overlay token + version, callers get deep copies, so a Hub PATCH is visible to the next `load_config()` everywhere (provider constructors, vision settings, ChatEngine). Voice honesty: deferred items remain listed as debt in row 20; nothing silently promoted. Tests at RC: Python **3124 passed, 3 skipped, 1 deselected** (`pytest -q`, after the last source change); Android **27 classes, 378 tests, 0 failures, 0 errors, 0 skipped**, freshness verified by comparing JUnit XML UTC timestamps against the newest `.kt` mtime across the UTC+7 offset - tests ran *after* the last Kotlin edit. Known non-blockers carried honestly: live Accessibility scenarios never driven on hardware (pre-existing); Phase 20 voice debt (owner-deferred). |
| 2026-08-24 | Backend, phase 11 part 1 | `.venv/Scripts/python.exe -m pytest -q` | **`memory.recall` was a dead owner control with four breaks**, found by checking a subagent's map against the source. (1) `memory_lines` gated on its own `recall_episodic: bool = True` parameter, not on the `recall_enabled` the builder wrote, and the sole caller passes no arguments - so the owner's value was stored and read by nobody. (2) `_reapply_memory` fetched the pipeline via `services.memory`, but `Services` keeps `pipeline` as a **sibling** (line 36 vs 31) and `services.memory` is the `MemoryManager` - a guaranteed `None`. (3) It sat in `apply`'s unconditional handler group, so `applied: ["memory.recall"]` came back even with no pipeline in the process - `applied` is a promise, in the code's own words. (4) The legacy `KeywordRetriever`/`NullRetriever` choice was build-time only, so even 1-3 fixed left the promise half true; `Services.knowledge.retriever` is a per-turn attribute, so it is now swapped live too. Two docstrings asserted the behaviour that never happened and were corrected. **The semantics nearly went the wrong way:** `config.yaml` scopes `recall` to keyword transcript search ("OFF ON PURPOSE") while the Hub calls it "Use memory in replies - look things up from past conversations" under **privacy**. The phone governs (S2: the settings screen is the owner's contract surface; a privacy reading beats a capability reading because being wrong that way puts past conversation content in a prompt the owner refused). **Behaviour change stated, not buried:** default is `recall: false`, so episodic recall is now off where it had been running against configuration - one toggle restores it, and `config.yaml`'s **value was not touched** (verified: it appears in the diff only as context). Red first at three levels, then the legacy half. **Mutation-tested both directions** because passing is not discriminating: gate forced closed -> 6 failed; gate forced to ignore the owner (the original bug) -> 2 failed, exactly the new negatives. **One of my own test discriminators was wrong and the suite caught it** - asserting `"migration" not in prompt` while the query was "how did the migration go", which also made the paired positive vacuous; both now use `"sqlite"`, present only in the stored episode. `2247 passed, 2 skipped, 1 deselected in 20.27s` from 2233: +14, 0 regressions. |
| 2026-08-24 | Android, phase 11 part 1 | not run | No Kotlin code changed. The Hub toggle and `MemoryConfigDto` already carried `memory.recall`; what changed is that flipping it now does something. |

## Verification log

| Date | Suite | Command | Result |
|------|-------|---------|--------|
| 2026-08-23 | Backend baseline | `.venv/Scripts/python.exe -m pytest -q` | `1817 passed, 1 skipped, 1 deselected` |
| 2026-08-24 | Backend, phases 2+3 | `.venv/Scripts/python.exe -m pytest -q` | `1846 passed, 1 skipped, 1 deselected in 19.33s` (+29 new) |
| 2026-08-24 | Android, phases 2+3 | `./gradlew --offline cleanTestDebugUnitTest testDebugUnitTest` | 22 classes, `312 tests, 0 failures, 0 errors, 0 skipped`, freshly executed after clean |
| 2026-08-24 | Backend, phase 1 | `.venv/Scripts/python.exe -m pytest -q` | `1879 passed, 1 skipped, 1 deselected in 29.82s` (+62 over baseline, 0 regressions) |
| 2026-08-24 | Android, phase 1 | `./gradlew --offline cleanTestDebugUnitTest testDebugUnitTest` | `classes=22 tests=312 skipped=0 failures=0 errors=0`, freshly executed after clean |
| 2026-08-24 | Backend, phase 4 | `.venv/Scripts/python.exe -m pytest -q` | `1965 passed, 1 skipped, 1 deselected in 32.91s` (+86 over baseline, 0 regressions) |
| 2026-08-24 | Backend, submit defect | `.venv/Scripts/python.exe -m pytest -q` | `1967 passed, 1 skipped, 1 deselected in 32.58s` (+88 over baseline, 0 regressions) |
| 2026-08-24 | Android, submit defect | `./gradlew --offline cleanTestDebugUnitTest testDebugUnitTest` | `classes=23 tests=319 skipped=0 failures=0 errors=0`, freshly executed after clean (312 -> 319) |
| 2026-08-24 | Android APK | `./gradlew --offline assembleDebug` | BUILD SUCCESSFUL, `app/build/outputs/apk/debug/app-debug.apk` 19,427,232 bytes |
| 2026-08-24 | Android, phase 9 | `./gradlew --offline cleanTestDebugUnitTest` then `./gradlew --offline testDebugUnitTest` | `classes=26 tests=359 skipped=0 failures=0 errors=0` (330 -> 359: 14 codec, 11 persistence, 4 repository). Clean and filtered runs must be separate invocations - `cleanTestDebugUnitTest` does not accept `--tests`. Freshness read off the JUnit XML `timestamp` attributes (`2026-08-24T04:40:44`-`04:40:46`) against `date -u` (`04:42:32`), not off BUILD SUCCESSFUL (S34). |
| 2026-08-24 | Android, phase 9 mutation | removed the `keep()` emptiness guard and made `restored()` return `emptyList()` | **6 of 10 failed** - the four load tests and both never-write-emptiness tests. Reverted, re-confirmed green. Tests were written alongside the implementation, so red was produced deliberately rather than observed. |
| 2026-08-24 | Android, phase 9 mutation | removed the `kept` equality guard from `keep()` | **1 of 11 failed**, exactly `a launch does not write back what it just read`. Reverted, re-confirmed green, `grep -c MUTATED` -> 0. |
| 2026-08-24 | Android APK, phase 9 | `./gradlew --offline assembleDebug` | BUILD SUCCESSFUL, `app/build/outputs/apk/debug/app-debug.apk` 19,427,232 bytes. Byte size identical to the previous two builds, so it was verified rather than assumed: opened as a zip, 12 dex files, `TranscriptStore` present in `classes4.dex` (823,540 B) and `classes6.dex` (65,688 B), sha256 prefix `e04b355d`. Genuine build; the size match is coincidence. |
| 2026-08-24 | Backend, phase 9 | `.venv/Scripts/python.exe -m pytest -q` | `2201 passed, 1 skipped, 1 deselected in 20.02s` - unchanged, as expected: phase 9 touched no Python. |
| 2026-08-24 | Backend, phase 5 | `.venv/Scripts/python.exe -m pytest -q` | `2030 passed, 1 skipped, 1 deselected in 28.75s` (+63 over the 1967 post-submit baseline: 50 planner, 11 wiring, 2 cross-language guard; 0 regressions) |
| 2026-08-24 | Android, phase 5 | not run | No Kotlin was modified in phase 5. The Android suite last ran green at `classes=23 tests=319` after the submit fix and is unaffected; this row records that rather than reusing that result as fresh evidence (S34). |
| 2026-08-24 | Backend, phase 6 | `.venv/Scripts/python.exe -m pytest -q` | `2093 passed, 1 skipped, 1 deselected in 19.14s`. Arithmetic: 2030 - 22 (tests relocated out of `test_planner.py`) + 85 (`test_task_graph.py`, incl. the 22 relocated + 6 parametrized cases) = 2093. 0 regressions. |
| 2026-08-24 | Android, phase 6 | not run | No Kotlin was modified in phase 6, confirmed by mtime: the two Kotlin files still dirty in the tree are 08:58/08:59, phase 6's work is 09:54. The prior green `classes=23 tests=319` is NOT reused as fresh evidence (S34). |
| 2026-08-24 | Backend, phase 10 red | `.venv/Scripts/python.exe -m pytest tests/test_temporal.py -q` | `5 failed, 81 passed` - `AttributeError: 'TemporalClock' object has no attribute 'use_timezone'`. The 22 phase-10 tests were written and run **before** any implementation existed, so red was observed rather than narrated (S33). |
| 2026-08-24 | Backend, phase 10 | `.venv/Scripts/python.exe -m pytest -q` | `2225 passed, 2 skipped, 1 deselected in 24.49s` from a 2201 baseline: +24 (22 red-first, plus 2 end-to-end HTTP tests added during self-review). 0 regressions. The second skip is `test_a_zone_is_not_lowercased`, which needs a real IANA key and calls `pytest.skip("no system timezone database")` on this tzdata-less host rather than pretending to pass. |
| 2026-08-24 | Android, phase 10 | `./gradlew --offline cleanTestDebugUnitTest` then `./gradlew --offline testDebugUnitTest` | `classes=26 tests=359 skipped=0 failures=0 errors=0`. Counts and freshness both read from `app/build/test-results/testDebugUnitTest/TEST-*.xml`, not from BUILD SUCCESSFUL (S34): clean finished `2026-08-24T05:33:52` UTC, test `timestamp` attributes span `05:34:00`-`05:34:02`, `date -u` `05:34:03` - inside the clean/now window, so genuinely fresh. Count is unchanged because the only Kotlin change was `assertEquals(49, ...)` -> `50` plus one named path in the existing test. |
| 2026-08-24 | Android APK, phase 10 | not run | No Kotlin production code changed in phase 10 - only `SettingsContractTest.kt` and three test-resource fixtures. Phase 9's APK stands; this row records that rather than reusing it as fresh evidence (S34). |
| 2026-08-24 | **S30 incident, phase 10** | `AURA_WRITE_ANDROID_FIXTURES=1 ... pytest tests/test_settings_fixture.py` | **Introduced a violation and repaired it.** The flag rewrites **all three** live fixtures from the host's live provider state, and a 182-byte `.env` at the repo root holds a real Gemini key that dotenv loads into `os.environ` at import time. Result: `provider_health.json` gained `active: "gemini"`, `chain: ["gemini"]`, `ready: true` (breaking `SettingsContractTest`'s keyless pin - `expected:<[]> but was:<[gemini]>`, 359 tests / 1 failed), and the **masked tail of the owner's real key** was written into `providers.json` (`"key_masked": "[REDACTED]"`) and `settings.json`. Repaired **without** `git checkout` (S45 forbids it, and it would also have reverted phase 1's legitimate `custom` provider additions) and without touching the owner's `.env`: two throwaway pytest modules `monkeypatch.delenv` every `brain.router.PROVIDER_KEYS` variable plus `CUSTOM_BASE_URL` **before** `init_runtime()` - before, necessarily, because the chain's `active`/`ready` are decided at build time while `source_of`/`masked` read `os.environ` at call time - then GET the three routes and rewrite each fixture in the same JSON shape. Both throwaways deleted. Verified: the key tail greps clean out of `android/app/src/test/resources/live/`, and all three fixtures differ from HEAD by **additions only** (18 / 21 / 6 lines). **Standing rule (corrected in phase 14):** never run that flag on a host with keys in `.env`. Deleting every provider variable before `init_runtime()` is **not sufficient** - probed in phase 14: every provider constructor calls `load_dotenv()` itself, which puts a deleted variable straight back, and a dump taken that way came back with `key_source: "environment"` and the real key's masked tail. A genuinely keyless runtime needs `load_dotenv` patched to a no-op in `brain.providers.{gemini,groq,mistral,http_chat}`, `server.main`, `core.config` *and* `dotenv` itself, before the delenv calls (verified keyless: `configured: False`, `key_source: ""`). When the change is small, prefer a targeted edit - section 45 asks for that anyway. Either way, diff every fixture against HEAD and confirm additions only. |
| 2026-08-24 | Backend, phase 10 addendum | `.venv/Scripts/python.exe -m pytest -q` | **Third S16 reach gap closed: the Android agent-tick prompt carried no time.** `_prepare`'s machine branch passed no `temporal`, `build()`'s tick branch dropped it, `_build_agent_prompt` never had the parameter. Live rather than theoretical: `input_text` takes free text and the owner's request arrives verbatim, so "hom nay" with no date in the prompt is a date the model invents (S16). 8 tests red first (`4 failed, 4 passed`, an `IndexError` splitting a prompt with no TIME section), then `2233 passed, 2 skipped, 1 deselected in 20.21s` (+8). **One intermediate failure, recorded not hidden:** `1 failed, 2232 passed` on `test_a_machine_turn_prompt_has_no_time_or_memory_section` - the pre-existing pin on the exact invariant this reverses. Its own docstring named the rule it enforced (*every section that exists to make Aura sound like herself is absent*) and TIME does not satisfy it: TIME is a world fact, the same category as the DEVICE STATE the tick has always carried, and the two were pinned together because they shipped together. So it was read before being touched, narrowed to its memory half (it lives in a *memory* integration file), renamed, and its docstring now records the reversal and the reasoning. MEMORY stays excluded and that half is right. `_build_time` reused rather than re-rendered, so the omit-when-empty rule keeps one definition and a clockless deployment gets a byte-identical tick prompt (pinned across `(None, [], [""], ["   "])`). |
| 2026-08-24 | Android, phase 10 addendum | not run | No Kotlin code changed - the fix is entirely in `brain/prompt_builder.py` and `brain/conversation.py`. The *prompt the phone receives* changes, which is behaviour S35 live testing would exercise and unit tests cannot. |
| 2026-08-24 | Backend, phase 11 part 2 | `.venv/Scripts/python.exe -m pytest -q` | **The semantic tier had no runtime caller.** `remember_user_stated`/`remember_user_correction` were written, tested and called by nothing outside the suite, so Aura could not keep a durable keyed fact about the owner from a conversation - the episodic tier saved the sentence and the fact it carried went nowhere. Closed with `RememberTool` (SAFE; every model-chosen argument validated; `CATEGORIES` added because the constants' "a typo is an ImportError" promise covers importing callers only). `2264 passed, 2 skipped, 1 deselected` (+17 from 2247, 0 regressions). **A real thread bug, caught by running through the executor rather than calling `execute` directly:** `call_with_timeout` uses a daemon thread and a SQLAlchemy SQLite session is thread-affine, so a threaded `remember` raises `ProgrammingError` every time, in production as in tests. Fixed with `timeout = 0`, the hatch `tools/timeout.py` documents for exactly this. |
| 2026-08-24 | **Mutation testing, phase 11 part 2** | targeted source mutations + full suite | **Two survivors; one was a real bug.** Dropping the pipeline argument from the single `_build_tools` call in `launcher/services.py` left the **entire suite green** - every test built its own executor, so none touched the one executor the process uses, and `remember` would have been absent from every real catalogue with 2263 tests agreeing. Closed by `test_the_composition_root_hands_the_pipeline_to_the_tools`, which drives the real `build_services`; the mutation now fails it. The second survivor - deleting `remember` from `config.yaml`'s `tools.allowed` - is **correct and was left alone**: it is the owner's documented off switch, and a test pinning their editable file would fail when they exercise a documented option (S2). Four mutations against the tool's own guards were all caught (category validation, empty-value guard, CONFIRMED downgraded to `infer()`, `timeout = 0` removed). Also fixed a discriminator that would have passed with the feature broken: `"remember" in prompt` matches the persona text *"remember what matters across conversations"* in every prompt Aura builds, so both directions now slice out the `TOOLS` section via `catalogue_of()`. |
| 2026-08-24 | Backend, phase 11 part 3 | `.venv/Scripts/python.exe -m pytest -q` | **A live S10 defect, found while looking for something else.** Part 3 set out to give the procedural tier a runtime caller and instead found that `brain/planner.py::same_app` could not recognise its own successes. It matches a squashed display name against a package, and **seven of ten** common apps missed when run against real package names before any code was written: Messenger `com.facebook.orca`, X `com.twitter.android`, Gmail `com.google.android.gm`, Play Store `com.android.vending`, Phone `com.google.android.dialer`, TikTok `com.zhiliaoapp.musically`, Messages `com.google.android.apps.messaging` (Google Maps, Photos and WhatsApp were fine). A miss makes `is_done` False for a launch that **did** happen, so the next tick re-issues OPEN_APP and the device opens an already-foregrounded app forever - the `open_app open_app open_app` loop S10 names by name, reached from the *recognition* side rather than the verification side phases 6-7 hardened. Fixed with `APP_ALIASES`: whole normalised name -> frozenset of **exact** packages, looked up before the substring heuristic. Both halves are load bearing - a substring reading of `"x"` matches nearly every package on a device, and a loose `messenger` entry would admit `com.facebook.katana`. The asymmetry decides it: a false positive advances a plan past a step that never happened and Aura then types into an app she is not in, while a missing entry falls back to the heuristic and is no worse off than before. One free function, six call sites (`planner.py:317`, `recovery.py:148,178,189`, `task_graph.py:204,234`), so the planner, recovery engine and task graph are fixed together - which is why the end-to-end tests ask `is_done`, what a tick actually asks, rather than `same_app`. Honest red first: 7 tests `NameError` on a missing import, then **3 failed / 32 passed** against the unmodified planner. `tests/test_planner.py` **50 passed**; full suite **2286 passed, 2 skipped, 1 deselected** (+22 from 2264, 0 regressions). |
| 2026-08-24 | **Mutation testing, phase 11 part 3** | targeted source mutations + `test_planner.py` | **Both caught, no survivors.** Deleting the alias lookup from `same_app` -> **10 failed**. Making it a substring match instead of exact-package -> **3 failed**, including the two negatives that matter most, Messenger->`com.facebook.katana` and Gmail->`com.google.android.apps.messaging`. Restored to 50 passed after each. The shape tests (`test_an_alias_does_not_make_matching_loose`, `test_an_alias_is_not_a_substring_rule`) and the five parametrized wrong-app negatives are what kill the second mutation; a suite testing only that each alias resolves would have let it through. |
| 2026-08-24 | Backend, phase 12 part 1 | `.venv/Scripts/python.exe -m pytest -q` | **A bus with no subscriber, and two docstrings claiming otherwise.** `events/log.py` closes it. Red first: 17 new tests in `tests/test_events.py` (17 -> 34). `2303 passed, 2 skipped, 1 deselected` (+17 from 2286, 0 regressions). **One of my own tests passed without exercising its path and I caught it, not the suite:** the unreadable-event test used a plain `class Hostile(Event)`, and a probe proved `getattr(Hostile(), "__dataclass_fields__", None)` is `{}` - inherited empty from the frozen base - so `describe` returned `""` before touching the hostile attribute and the `except` branch never ran. Now a real `@dataclass(frozen=True)` with a `boom` field, plus an assert on the log text. |
| 2026-08-24 | **Mutation testing, phase 12 part 1** | targeted source mutations + full suite | **5 of 6 caught; the survivor was dead code.** Root unwired -> 2 failed; Enum checked after `str` -> 1 failed (exactly the `(str, Enum)` trap); default-allow instead of default-deny -> 2 failed; `QUIET_EVENTS` emptied -> 3 failed; `ErrorEvent` demoted from WARNING -> 2 failed. `SKIP_FIELDS = frozenset()` left all 34 green, **correctly**: a dict falls through to the generic `arguments=<dict>` branch whether or not a skip list names it, so `SKIP_FIELDS` was a second mechanism for a guarantee the generic branch already made. Deleted rather than tested, with the reasoning written into the surviving branch. Also recorded: my first attempt at the Enum mutation was **not a real mutation** - it inserted an opted-in-only `str` check above Enum, which never covers the redaction path - and read as a survivor at 34 passed. A survivor must be checked for being a viable mutation before it is believed. |
| 2026-08-24 | Backend, phase 12 part 2 | `.venv/Scripts/python.exe -m pytest -q` | **The cognitive layer got a voice, and the first version had a real bug in it.** `_announce` opened with `if was == node: return`, correct for a repeating tick and wrong because **`task_node == ""` means two different things** - not started, and finished. A device reporting a task already complete on its *first* tick therefore had its completion silently dropped: `"" -> ""` reads as "nothing moved". Found by a failing test, diagnosed with a probe that confirmed the graph genuinely reported `is_finished: True, is_stuck: False, current: None`, so the defect was mine and not the graph's. Fixed with a second reading rather than a special case - `had_plan`, taken **before** `set_plan` writes it - so *arriving* at "no current node" is distinguishable from sitting there; both reads now precede both writes. A second failure was the opposite: my test asserted `total == 6` for `plan_for("open YouTube and search Minecraft")`, and a probe proved the plan has **5** steps - the test was wrong, not the code, and it was corrected rather than accommodated. `tests/test_machine_turns.py` **89 passed**; full suite `2313 passed, 2 skipped, 1 deselected` (+10 from 2303, 0 regressions). |
| 2026-08-24 | **Mutation testing, phase 12 part 2** | targeted source mutations + `test_machine_turns.py` | **Every viable mutation caught, two only after the tests were strengthened.** Repeat guard removed -> 1 failed; arrival guard removed -> 1 failed; `had_plan` term dropped -> 1 failed; `_announce` unwired -> 5 failed; stuck never announced -> 1 failed; stuck published as finished -> 2 failed; stuck loses its step -> 1 failed. **Two survivors, two different lessons.** `index` replaced by a constant `0` survived because the only test looked at a *first* tick, where the right index really is zero - a missing assertion, now a second tick pinning `index == 1` after a verified launch. `elif graph.is_stuck:` -> `or True` survived and **should have**: `is_stuck` means exactly "no current node and not finished" and `_plan` returns before `_announce` on an empty plan, so the disjunct cannot change an answer there. But asking *why* it survived exposed that the stuck branch had **no test at all** - the equivalence was hiding a coverage hole, and two tests now cover it. A surviving mutation has three readings, not two: a missing test, dead code, or an equivalence concealing a missing test. Phase 12 hit all three. |
| 2026-08-24 | Android, phase 11 part 3 | not run | No Kotlin changed. The work is `brain/planner.py` and `tests/test_planner.py`. The phase 9 APK (`android/app/build/outputs/apk/debug/app-debug.apk`, 19,427,232 bytes) remains current; S35 live device scenarios are still owed and the alias fix is exactly the kind of thing they would exercise. |
| 2026-08-24 | Android, phase 11 part 2 | not run | No Kotlin changed. The work is `tools/builtins/memory.py` (new), `tools/factory.py`, `launcher/services.py`, `memory/user_model.py`, `config.yaml` and two test files. |
| 2026-08-24 | Backend, phase 13 | `.venv/Scripts/python.exe -m pytest -q` | **`2354 passed, 2 skipped, 1 deselected`** (+41 from 2313, 0 regressions), reached in four verified steps as each class landed: 2313 -> 2330 -> 2339 -> 2350 -> 2354. `tests/test_proactive.py` alone **106 passed**. The 41 are `TestGreetingsSurviveARestart` (7), `TestReadingTheLedgerFile` (9), `TestPresenceSurvivesARestart` (7), `TestTheMessageStoreKnowsWhenSomebodySpoke` (8), plus 3 module-level wiring tests and the 7 of `TestTheProactiveLimitsSurviveARestart`. Each class carries a banner naming the section it defends. `data/` still holds only `memory.db` afterwards - checked, because a ledger test that wrote to the real data directory would pass and pollute; the wiring test asserts `ledger.path.parent == DATA_DIR` (a path, never a send). |
| 2026-08-24 | **S44 defect, phase 13: wired, green and dead** | probe, not a test | **`MemoryManager.last_said_at` was invisible to the primary client and every test passed.** It was written `session_id: str = "default"`, copying `get_recent` directly above it - but an Android install supplies its own session id (`server/session.py:66-83`: "Callers supply their own ids"), so the method could not see a single message the owner had ever sent from their phone. Every test passed because every test used the default session, the very thing being copied. Two lines of probe against a realistic input found it: `last_said_at()` -> `None` beside `last_said_at(session_id="android-9f2c41")` -> a real datetime, same database. Fixed to `session_id: str \| None = None` (every session), justified from `server/runtime.py:323-337`'s single-tenant note - "the auth token is the identity boundary, and the only one" - and pinned by `test_it_finds_the_owner_whichever_client_they_used` plus mutation N3. **Standing rule: a default that matches the file above it can still exclude the primary client.** Copy the idiom, re-derive the default. |
| 2026-08-24 | **Mutation testing, phase 13** | targeted source mutations + `test_proactive.py` / `test_memory*.py` / full suite | **31 mutations across five files, 31 caught, 0 survivors.** Covered: the ledger's atomic write, version root, per-row validation and each of the four row-level rejections; `ProactivePolicy`'s load-on-init, `_save` from `note_sent`, `_save` from `reset` (a reset that left the file behind is a permanent ban); the derived `_greeted_this_part` in both directions (date term dropped, `part_of_day` term dropped, category filter dropped); `_presence` live-signal precedence and its exception guard; `last_said_at`'s role filter, session filter, `desc` -> `asc`, and `parse_timestamp` result; and both composition-root pass-throughs in `launcher/services.py`. **Two process notes.** `desc` -> `asc` survived a first draft because every test had exactly one user row, where oldest and newest are the same answer - closed by `test_it_answers_with_the_most_recent_turn_not_the_first` with two rows and explicit timestamps (`save` stamps at second precision, so the rows are inserted directly). And two anchors matched twice because the docstrings quote the code they explain; re-run with unique multi-line anchors rather than recorded as survivors - a survivor must be proved viable before it is believed. |
| 2026-08-24 | Android, phase 13 | not run | No Kotlin changed. The work is `proactive/ledger.py` (new), `proactive/policy.py`, `proactive/engine.py`, `memory/manager.py`, `launcher/services.py`, `.gitignore` and `tests/test_proactive.py`. `NotificationWorker.kt` was **read and found already correct**, which is the S19 evidence rather than a change. The phase 9 APK (19,427,232 bytes) remains current; S35 live device scenarios are still owed. |
| 2026-08-24 | Backend, phase 14 | `.venv/Scripts/python.exe -m pytest -q` | `2387 passed, 2 skipped, 1 deselected` (2379 -> 2387: +8 in `tests/test_companion.py`, 5 closing the age-arithmetic mutation gaps and 3 driving a real PATCH against a running gate; 0 regressions) |
| 2026-08-24 | Backend, phase 14 target suite | `.venv/Scripts/python.exe -m pytest tests/test_companion.py -q` | `110 passed` (102 -> 110). The three new ones live in `TestTuningTheGateReachesTheRunningGate` and use the real `api` fixture from `tests/test_settings_api.py`, because the point is that a PATCH reached the running gate rather than only the disk. |
| 2026-08-24 | **Mutation testing, phase 14** | 28 targeted source mutations across `companion/policy.py`, `companion/engine.py`, `core/settings_store.py`, `core/config.py`, `server/settings_service.py` + `test_companion.py` | **Round one: 19 of 28 caught.** Survivors clustered in the age arithmetic across the disk boundary and, once those were closed, **two in the live-reapply path** - both of which survived because the tests asserted *membership in `LIVE_PATHS`* rather than driving a PATCH and reading the running policy. Same shape as the phase 11 `memory.recall` defect. **Round two after +8 tests: 28 of 28 caught, 0 survivors.** Four anchors were mis-transcribed on the way and reported as `ANCHOR MISSED x0` rather than passing silently - the harness was built to distinguish a missed anchor from a real survivor, which is the only reason those four were not counted as clean kills. |
| 2026-08-24 | Android, phase 14 | `./gradlew --offline cleanTestDebugUnitTest` then `./gradlew --offline testDebugUnitTest` (separate invocations, per S34) | `classes=26 tests=359 skipped=0 failures=0 errors=0`. Freshness proven from the JUnit XML `timestamp` attributes, not from BUILD SUCCESSFUL: newest suite stamped `13:08:37` against a wall clock of `13:08:57`. |
| 2026-08-24 | **S45 fixture correction, phase 14** | targeted edit, not regeneration | The two Android fixture failures were the fixture *correctly* noticing a deliberate server change - six new settable `server.companion.*` paths and one new `effective` field - not a defect. Regenerating was attempted first and **the keyless recipe in the S30 standing rule did not work**: a throwaway that deleted every `PROVIDER_KEYS` variable before `init_runtime()` still emitted a real key tail, `"configured": true`, `"key_source": "environment"`. Cause found by grep: **every provider constructor calls `load_dotenv()` itself** (`server/main.py:19`, `brain/providers/gemini.py:29`, `groq.py:42`, `http_chat.py:260`, `mistral.py:44`), so the deletion is undone the moment the provider is built. The throwaway and its output directory were deleted immediately, the rule in this file was rewritten to the *probed* recipe (patch `load_dotenv` to a no-op in those four modules plus `server.main`, `core.config` and `dotenv` itself - verified to yield `configured: False, key_source: ''`), and the fixture was edited in place, which S45 prefers anyway. Additions-only, proven by `git diff --stat`: 52 insertions, 0 deletions across the three live fixtures. |
| 2026-08-24 | Android contract test, phase 14 | `SettingsContractTest.kt` | `assertEquals(50, configurable.size)` -> `56`, plus all six new paths named individually. No DTO change was needed: `CompanionConfigDto` (`ControlDto.kt:240`) carries only `enabled` and `ApiFactory.kt:34` sets `ignoreUnknownKeys = true`, so the phone tolerates the new `effective` field and locks its controls on the `configurable` list. |

| 2026-08-24 | Backend, phase 15 | `.venv/Scripts/python.exe -m pytest -q` | `2407 passed, 2 skipped, 1 deselected in 22.46s` (2387 -> 2407: +20 in `tests/test_proactive.py`, 0 regressions). The 20 are four new classes - a category needs a cooldown to send (4), the appreciation category can actually fire end to end (4), the source only says what the user said (9), nothing on the bus acts on an event (3) - and the seven `check_in` tests were repaired in place rather than added to. |
| 2026-08-24 | Backend, phase 15 target suite | `.venv/Scripts/python.exe -m pytest tests/test_proactive.py -q` | `126 passed in 1.60s` (106 -> 126). |
| 2026-08-24 | **Backend, phase 15 red checks** | back the fix out, run the two affected classes | **Both read `2 failed, 2 passed`, which is the discrimination the tests are for.** Setting `memories = None` in `_build_proactive` fails exactly the two `build_services` wiring tests while the two that inject their own source still pass. Removing the `KNOWN_CATEGORIES` guard from `allows()` fails exactly the two unknown-category tests while the two listed-category tests still pass. A red check that took down the whole class would not have shown that the fix, not the fixture, is what each test measures. |
| 2026-08-24 | **Backend, phase 15 boundary test fires** | attach a synthesised acting handler | `TestNothingOnTheBusActsOnAnEvent` was proven to fail, not merely to pass: a fake `tools.autopilot` module with a handler that acts was attached to the built bus, and both boundary tests caught it (`A subscriber outside the presentation layer is on the event bus: ['tools.autopilot']`). A boundary test that has never been seen to fail is a boundary test of unknown value. |
| 2026-08-24 | **Mutation testing, phase 15** | 15 targeted source mutations across `proactive/memories.py`, `proactive/policy.py`, `launcher/services.py` + `test_proactive.py` | **Round one: 14 of 15 caught. One survivor, and it was a real gap.** `if when is None: continue` -> `pass` in `EpisodicMemorySource` had no test, and its consequence is out of proportion to its size: `parse_timestamp` is deliberately tolerant because it reads columns written by several versions of Aura, so without the `continue` the next comparison is `oldest <= None`, which raises `TypeError`, which `_gather_memories()` swallows as "nothing to say" - the cost of one bad row is not one lost memory but **every** memory for as long as that row sits inside the fortnight the query covers. `test_one_unreadable_row_does_not_silence_the_good_ones` closes it, putting a bad row (`occurred_at="sometime last tuesday"`) in front of a good one - the bad row sorts first without arranging, because `by_category` sorts a string column and that text sorts above any ISO date. **Round two: 15 of 15 caught, 0 survivors.** |
| 2026-08-24 | Android, phase 15 | not run | No Kotlin changed. The work is `proactive/memories.py` (new), `proactive/policy.py`, `proactive/context.py`, `launcher/services.py` and `tests/test_proactive.py`. The phase 9 APK (19,427,232 bytes) remains current; S35 live device scenarios are still owed. |

| 2026-08-24 | Backend baseline before phase 16 | `.venv/Scripts/python.exe -m pytest tests/test_tool_framework.py tests/test_tools.py tests/test_tool_settings.py -q` | `115 passed` before any edit, so every later number is measured against a known-green start rather than assumed. |
| 2026-08-24 | **Backend, phase 16 red-first** | run the new tests against unmodified source | **Framework `4 failed, 4 passed`; remember `7 failed, 1 passed` (`AttributeError: 'RememberTool' object has no attribute 'verify'`).** The four framework passes are the informative half: a tool with a failing `verify()` still passed because **nothing consulted it**, which is the defect stated as a test result. |
| 2026-08-24 | Backend, phase 16 target suites | `.venv/Scripts/python.exe -m pytest tests/test_tool_framework.py tests/test_tools.py tests/test_tool_settings.py -q` | `232 passed` (115 -> 232 counts the new tests plus the files' full collection; the 16 added are 8 in `test_tool_framework.py` and 8 in `test_tools.py`). |
| 2026-08-24 | **Backend, phase 16 end-to-end probe** | drive `ToolExecutor.execute('remember', ...)` on a real `MemoryPipeline`, then count rows | **The defect and the fix, both observed at the executor boundary rather than at the tool.** Before: `key='名前'` -> `ok=True`, `output='remembered 名前 = Thien'`, `rows=0`. After: `名前` and `???` both -> `ok=False` with a reason naming the key, while `identity.name` and `identity.tên` -> `ok=True` and `rows=2`. The Vietnamese key is in the probe on purpose: it proves the guard rejects only keys that genuinely cannot be stored, not every non-ASCII key an owner writing Vietnamese would reasonably use. |
| 2026-08-24 | **Mutation testing, phase 16** | 5 targeted source mutations across `tools/executor.py` and `tools/builtins/memory.py` | **5 of 5 caught, 0 survivors - every guard is load-bearing.** M1 executor returns `_run`'s result and never calls `verify` -> 4 failures. M2 `execute` discards the `None` return again, i.e. the original defect restored -> caught. M3 `verify` returns `ok(...)` unconditionally instead of reading back -> caught. M4 executor swallows a raising `verify` and keeps the success (fail **open**) -> caught. M5 executor re-verifies results that already failed -> caught. Originals were saved before each mutation and restored after, and the restoration was proven by the full-suite pass that followed. |
| 2026-08-24 | Backend, phase 16 | `.venv/Scripts/python.exe -m pytest -q` | `2423 passed, 2 skipped, 1 deselected in 22.42s` (2407 -> 2423: +16, 0 regressions). |
| 2026-08-24 | **Backend, phase 16 reachability at the real composition root** | `build_tools(load_config()['tools'], memory=MemoryPipeline())`, then execute through it | **The guard is reachable in production, not only in tests - checked because the wiring had the exact shape of the phase 15 defect.** `build_tools(memory=None)` defaults to `None` and a `None` pipeline means `RememberTool` is **not registered at all**, which is the same "constructor parameter production never passes" trap `_build_proactive` fell into. It is wired: `launcher/services.py:133` passes `pipeline` into `_build_tools`, which passes `memory=pipeline` at :696, with a comment saying the ordering is load-bearing. Probed against the owner's real `config.yaml`: `allowed` is `['current_time', 'remember']`, `available()` returns both, so `remember` clears all five gates (SAFE, and `auto_approve: [safe]`). Then through the executor: `名前` and `???` -> `ok=False` with the reason naming the key, `identity.name` -> `ok=True` and `value_of` reads back `'Thien'`. A temporary `AURA_DB_PATH` was used so nothing touched the real database, and no key or secret was printed. |
| 2026-08-24 | Android, phase 16 | not run | No Kotlin changed. The work is `tools/executor.py`, `tools/base.py` (docstring), `tools/builtins/memory.py`, `tests/test_tool_framework.py`, `tests/test_tools.py`. The phase 9 APK (19,427,232 bytes) remains current; S35 live device scenarios are still owed. |
| 2026-08-25 | Android, phase 17 baseline | `./gradlew --offline testDebugUnitTest` | 26 classes, `359 tests, 0 failures, 0 errors, 0 skipped` - **but the JUnit XML timestamp read `13:08:37`, i.e. stale.** Recorded as the baseline count only; S34 forbids trusting an UP-TO-DATE task, and every later run in this phase adds `--no-build-cache` after `cleanTestDebugUnitTest` and is accepted only on a fresh timestamp. |
| 2026-08-25 | **Android, phase 17 cache trap observed** | `./gradlew --offline cleanTestDebugUnitTest testDebugUnitTest` after reverting a mutation | **`BUILD SUCCESSFUL in 2s` with the JUnit timestamp unchanged at `00:24:29` - the task came back FROM-CACHE and the "result" was the previous run's XML.** This is the failure mode S34 names. Fixed by adding `--no-build-cache` to every subsequent invocation; the proof of a real execution is a fresh timestamp plus a bare `> Task :app:testDebugUnitTest` with no FROM-CACHE / UP-TO-DATE suffix. |
| 2026-08-25 | Android, phase 17 (activity field + wire tests) | `./gradlew --offline --no-build-cache cleanTestDebugUnitTest testDebugUnitTest` | 26 classes, `368 tests` then `370 tests, 0 failures, 0 errors, 0 skipped`, timestamps `17:10:59` and `00:25:22` - freshly executed. 359 -> 370 is +9 decision tests for `activityFor`/`windowFrom` and +2 wire-contract tests. |
| 2026-08-25 | Android, phase 17 (stop-early heuristics) | `./gradlew --offline --no-build-cache cleanTestDebugUnitTest testDebugUnitTest` | 26 classes, `374` then **`378 tests, 0 failures, 0 errors, 0 skipped`**, timestamps `00:38:18` and `00:44:52` - freshly executed. `AccessibilityAgentTest` 25 -> 34 tests; `ForegroundActivityTest` 9 -> 11. |
| 2026-08-25 | **Backend, phase 17 pre-code probe** | drive `brain/planner.py::plan_for` on twelve phrasings, compare against the device's two heuristics extracted from Kotlin | **Twelve concrete truncations, proved against the real planner before any code was written.** Six reached `shouldAutoComplete` (`mở YouTube tìm nhạc`, `open YouTube search for Minecraft`, ...) and six reached `isSearchTaskComplete` (`search Minecraft then play`, ...): the planner builds two nodes, the device says single-step. The same probe extracted `multiStepKeywords` and `CONJUNCTIONS` from source and disproved the planner's comment claiming they are "deliberately the same set". |
| 2026-08-25 | Backend, phase 17 | `.venv/Scripts/python.exe -m pytest -q` | **`2428 passed, 2 skipped, 1 deselected in 40.16s`** (2423 -> 2428: +5 cross-language invariants in `tests/test_agent_protocol.py`, 45 -> 50 tests, 0 regressions). Both generated corpora were checked to actually reach their assertions - `multi_step_phrasings()` 80/80 and `trailing_clause_phrasings()` 192/192, none skipped - because a generator that yields nothing is a green test that tests nothing. |
| 2026-08-25 | **Mutation testing, phase 17** | 9 targeted source mutations across `AuraAccessibilityService.kt`, `AccessibilitySnapshot.kt`, `AuraActionExecutor.kt` and `tests/test_agent_protocol.py` | **9 of 9 caught, 0 survivors.** M1 drop `activityFor`'s package-match guard -> 1 failure, and precisely the cross-app test. M2 drop the blank guard -> 3. M3 drop `.trim()` -> 3. M4 rename the wire key to `"screen"` -> **2, and only the two contract tests**, which is the measurement of what the nine decision tests did not cover. M5 reduce the early exit to conjunctions only, i.e. defect 1 restored -> 1 failure naming `'open YouTube search for Minecraft'`. M6 remove the search-verb deferral -> 3 Kotlin + 1 Python (the Python one is the structural body guard, proving it is not vacuous). M7 drop `"tìm "` from `SEARCH_VERBS` -> 2 Python. M8 remove the trailing-clause deferral -> 1 Kotlin + 1 Python. M9 measure from the first search verb instead of the last -> 1 Kotlin. Each original was saved before mutation, restored after, and confirmed **byte-identical with `diff -q`**. |
| 2026-08-25 | Backend, phase 18.1 | `.venv/Scripts/python.exe -m pytest -q` | `2513 passed, 2 skipped, 1 deselected in 32.44s` (+85 over the 2428 pre-phase count, 0 regressions) |
| 2026-08-25 | Phase 18.1 unit | `.venv/Scripts/python.exe -m pytest tests/test_pc_tools.py -q` | `85 passed in 6.56s` |
| 2026-08-25 | Phase 18.1 mutation | two hand-built batteries over `system.py`, `desktop.py`, `factory.py`, `config.yaml` | `12 of 12 caught, 0 survivors` and `21 of 21 caught, 0 survivors`; 3 survivors found and fixed first; originals restored and confirmed byte-identical with `diff -q` |
| 2026-08-25 | Backend, phase 18.2 | `.venv/Scripts/python.exe -m pytest -q` | `2667 passed, 2 skipped, 1 deselected in 64.68s` (+154 over the 2513 pre-phase count, 0 regressions) |
| 2026-08-25 | Phase 18.2 unit | `.venv/Scripts/python.exe -m pytest tests/test_command_tool.py -q` | `154 passed`. Three of them spawn real programs on purpose - a value containing `&&` reaching a program as one argument rather than as two commands, a batch file being refused before cmd.exe can re-parse a quote, and an overrunning command being stopped along with its children instead of holding the reply open. A mock can only assert what I already believed about the platform. |
| 2026-08-25 | Android, phase 18.2 | `gradlew.bat cleanTestDebugUnitTest testDebugUnitTest --no-build-cache` | **`378 tests, 0 failures, 0 errors, 0 skipped`**, BUILD SUCCESSFUL, with `> Task :app:testDebugUnitTest` carrying no FROM-CACHE / UP-TO-DATE suffix - genuinely executed, per section 34. Covers the new `ToolsConfigDto.commands` field and the three `SettingsContractTest` section 2 guards. |
| 2026-08-25 | **Phase 18.2 batch-file probe** | nine payloads through a real `.bat`, the same nine through a real `.exe`, `shell=False`, Python 3.11.15 | **The 18.2 brief's claim was too broad, and the correction decides how wide the refusal has to be.** `& && \| ^ >` alone created no canary - the CVE-2024-1874 defensive quoting is present. A literal `"` in the value **breaks out** and the remainder runs as commands (both quote-bearing payloads created their canaries), and `%CD%` / `%PATH:~0,12%` **expand** inside the batch file, so the value the program receives is not the value supplied. The identical payloads against an `.exe` arrive in `sys.argv` byte-for-byte with nothing created and nothing expanded; `.cmd` behaves exactly like `.bat`. Hence: refuse a resolved `.bat`/`.cmd` **that has a fillable slot**, allow one that has none. |
| 2026-08-25 | **Phase 18.2 timeout probe** | a `.bat` leaving a detached `ping -n 40` grandchild, `subprocess.run(timeout=1.0)` vs temp files + `wait`/`kill`/`taskkill /F /T /PID` | **29.25 s vs 1.08 s.** The grandchild inherited the stdout pipe and held it open long after `cmd.exe` died, and `taskkill /F /T` did not unblock the read. Temp files return in ~1 s **with the output already produced captured**, which is why an overrun reports partial output plus the fact that it was stopped rather than holding the reply open. |
| 2026-08-25 | Phase 18.2 mutation | 30 hand-built mutations over `tools/builtins/commands.py`, `tools/factory.py`, `config.yaml`, `core/settings_store.py` | **30 of 30 caught, 0 survivors, 0 bad anchors.** Three survived the first run and each was a real verification gap rather than an equivalent mutant: a refusal that survived because a *different* branch caught the input (leaving the actionable message untested), an import whose only value is *future* provider names (no current name could test it - closed with an `ACME_SESAME` monkeypatch), and a factory gate whose real job is the truthfulness of a startup log line rather than the registration. `.mutbak` backups restored in a `finally` block; residue checked afterwards - no `.mutbak` files, no canaries, all four mutated files parse, harness deleted. |
| 2026-08-25 | Backend, phase 18.3 | `.venv/Scripts/python.exe -m pytest -q` | **`2786 passed, 3 skipped, 1 deselected`** (+119 over the 2667 pre-phase count, 0 regressions). The third skip is new and is the symlink-containment test: its reason names `[WinError 1314] A required privilege is not held by the client` rather than passing quietly, and the same escape is covered unskipped by the junction test. |
| 2026-08-25 | Phase 18.3 unit | `.venv/Scripts/python.exe -m pytest tests/test_filesystem_writes.py -q -rs` | **`119 passed, 1 skipped in 0.63s`**. The cross-volume test **ran** on this host rather than skipping (`%TEMP%` on C:, repository on D:); on a single-volume machine it skips and only the structural assertion about `dir=str(target.parent)` runs. |
| 2026-08-25 | **Phase 18.3 cross-volume probe** | `os.replace` from a `%TEMP%` temporary (C:) onto a target in the repository (D:), CPython 3.11.15 | **`OSError 17, ERROR_NOT_SAME_DEVICE`.** CPython calls `MoveFileExW` without `MOVEFILE_COPY_ALLOWED`, so a temporary in the system temp directory would have made **every** write to a folder on any other drive fail. Measured because a mutation survived, not because a test failed - pytest puts `tmp_path` under `%TEMP%`, so the same-volume case always holds there and no `tmp_path`-based test can observe this. The `dir=` in `tempfile.mkstemp` is load-bearing, not tidiness. |
| 2026-08-25 | **Phase 18.3 junction probe** | `mklink /J` unelevated, then `Path.resolve()` through the junction, then `shutil.rmtree` over the containing directory | **Junction created with no privilege; `resolve()` sees through it identically to a symlink; `rmtree` follows it into its target.** All three answers changed the test: symlinks need `SeCreateSymbolicLinkPrivilege` and skip here, which would have left the phase's single most important property permanently unexercised on this host - so containment is proved against a **junction** instead, with `os.rmdir(link)` in the test's own `finally` because pytest reaps old `tmp_path` directories with `rmtree` and would otherwise delete the target it was meant to protect. Verified `target intact: True` after cleanup. |
| 2026-08-25 | Android, phase 18.3 | `gradlew.bat cleanTestDebugUnitTest testDebugUnitTest --no-build-cache --console=plain` | **`27 suites: 378 tests, 0 failures, 0 errors, 0 skipped`**, counted from the JUnit XML rather than trusting the `BUILD SUCCESSFUL` line. Same total as 18.2: the three new `writable_paths` assertions live inside existing `SettingsContractTest` cases. |
| 2026-08-25 | Phase 18.3 mutation | 22 hand-built mutations over `tools/builtins/filesystem.py`, `tools/factory.py`, `core/config.py`, `config.yaml` | **22 of 22 caught, 0 survivors** after two rounds; **20 caught first pass, and both survivors were real gaps rather than equivalent mutants - and both were defects in *my own tests*, not the code.** (1) "the temporary is named after the target" survived because my decoy sat at `notes.tmp` while the repository's `with_suffix(suffix + ".tmp")` idiom produces `notes.md.tmp` - so the test named a file no plausible implementation would touch and could never fail; it now plants both spellings and asserts `is_file()` as well as bytes, since the mutant renames the temporary away rather than overwriting. (2) "the temporary is written in the system temp directory" survived and was a genuine production bug - see the cross-volume probe row. Two bad anchors found and fixed by reading the real source with `sed` rather than guessing it (one matched 0 times, one matched 2). Both source files confirmed byte-restored afterwards (`grep -c "expanduser().resolve()"` = 2, no `if False`, no stray `import io`), harness deleted. |
| 2026-08-25 | **Phase 18.3 Kotlin mutation** | misspell `@SerialName("writable_paths")` as `("writeable_paths")`, then `cleanTestDebugUnitTest testDebugUnitTest --no-build-cache` | **`SettingsContractTest > settings parse into the sections the hub renders FAILED`, `BUILD FAILED`** - so the device-side assertions are load-bearing. They were **not** before this: all three would have passed with the name misspelled, because `writablePaths` defaults to an empty list and every body asserted against was empty, making empty-because-absent and empty-because-defaulted indistinguishable. Fixed by putting a real writable root in the inline JSON body and reading the value back, which closed the **identical pre-existing gap for `allowed_paths`** at the same time. Restored from a `.backup` file. |
| 2026-08-25 | Phase 18.3 fixture | `.venv/Scripts/python.exe -m pytest tests/test_settings_fixture.py -q` | Failed first, exactly as predicted in the 18.3 brief before the code was written: `settings.json is missing fields the server now sends: ['effective.tools.writable_paths']`. Fixed by a **one-line targeted edit** to `android/app/src/test/resources/live/settings.json`, never a regeneration - `AURA_WRITE_ANDROID_FIXTURES=1` rewrites fixtures from this host's live state and there is a real Gemini key in `.env` at the repository root, which is the section 30 violation that already happened once in phase 10. Section 45 asks for targeted edits anyway. |
| 2026-08-25 | Phase 18.4 dependency probe | `import mss` and `PIL.__version__` under `.venv/Scripts/python.exe` | **`mss` is NOT installed; pillow IS (11.1.0)** despite both being listed optional. This inverted the phase's plan. Reusing `ScreenshotCapture` was impossible, so `GdiScreenCapture` was written; and PIL being present but optional is why `encode_png` uses `zlib` instead - a screenshot that needed pillow would be the one PC capability an owner could not use without installing something. It also revealed that `capture_screen: true` had been a no-op on this host, since `_build_vision` tried only `mss`. |
| 2026-08-25 | **Phase 18.4 correctness probe** | `GdiScreenCapture().capture()` encoded with `encode_png` and decoded with PIL, compared against `ImageGrab.grab()` and against that reference flipped and red/blue swapped | **`sampled 2400 pixels: 2400 identical, worst channel delta 0`; flipped reference 90.3 percent close; swapped reference 4.9 percent close.** The reason this probe exists at all: a mirrored image and a channel-swapped image both have the byte count the naive check would accept, so the only honest verification is someone else's implementation of the same capture. Now a test, not a measurement - and the only test that catches the `biHeight` mutation. Also confirmed `encode_png` on a hand-built 2x2 decodes in PIL to exactly `[(255,0,0),(0,255,0),(0,0,255),(255,255,255)]`. |
| 2026-08-25 | Phase 18.4 DPI probe | `SetThreadDpiAwarenessContext(-4)` then `GetThreadDpiAwarenessContext()` before and after | **Previous context `2147508240`, restored.** Confirmed reversible before being relied on, and the process-wide variant was rejected on the strength of it: permanent, and it changes how every other thing in the process reads the screen. The *benefit* is unverifiable here - one display at 100 percent - so what is tested is the discipline (undo after success, undo on the failure path, and the undo is a real restore rather than a no-op) and the payoff is recorded as unproven. |
| 2026-08-25 | Phase 18.4 handle probe | `GetGuiResources(GetCurrentProcess(), GR_GDIOBJECTS)` before and after 8 captures | Stable within the tolerance of 2, so the `finally` releasing the screen DC, the memory DC and the bitmap does its job. Kept as a test because a per-capture GDI leak is invisible in one call and fatal over a session that captures every two seconds; each of the three releases has its own mutation and each is caught by this one test. |
| 2026-08-25 | Phase 18.4 registration | `build_registry({})` and `build_registry({'writable_paths': [tmp]})` | Shipped config gives `['current_time','focus_window','list_processes','list_windows','system_information']` - no `take_screenshot`. A writable root adds the four 18.3 writers **and** `take_screenshot`. With `default_screen_capture` monkeypatched to None the writers still register and the screenshot does not, with the reason logged. The config guards read `config.yaml` from disk rather than through `load_config()`, so a defaulting bug in the loader cannot hide a silent enable. |
| 2026-08-25 | Phase 18.4 unit tests | `.venv/Scripts/python.exe -m pytest tests/test_screenshot.py -q` | **112 passed.** Weighted towards the three things no byte count can see: that the picture is a picture (independent decoder, plus flipped and swapped references that must match worse), that nothing is captured on a refused path (a counting fake capture, `captures == 0`), and that the image only lands inside a granted root (traversal, absolute, and a directory junction, with the file outside checked byte-for-byte afterwards). |
| 2026-08-25 | Phase 18.4 full suite | `.venv/Scripts/python.exe -m pytest -q` | **`2898 passed, 3 skipped, 1 deselected in 43.22s`** - 112 more than 18.3's 2786 and no regressions. No new config keys, so no Android fixture change was needed and `tests/test_settings_fixture.py` still passes untouched. |
| 2026-08-25 | **Phase 18.4 mutation** | 48 hand-built mutations over `vision/capture.py`, `tools/builtins/screen.py`, `tools/factory.py` | **48 of 48 caught, 0 survivors** after fixes; **43 caught first pass, and of the 5 survivors 2 were my own mis-specified mutations while 3 were real defects in my own tests.** (1) `test_true_is_not_display_one` **could never fail**: it asserted `_resolve(monitor=True) == PRIMARY_DISPLAY`, and `PRIMARY_DISPLAY` is 1 while `True == 1` in Python, so using the bool directly as an index satisfied it - the same class of defect 18.3's battery found. Replaced with a `type(resolved) is int` check plus the two cases where the guard is observable: `monitor: no` is `int(False)` is 0, a different screen from the fallback, and a bool must produce the configuration warning rather than quietly work. (2) The DPI restore test **measured its baseline too late** - the first capture of the session leaves the thread aware, so with the restore removed it compared an already-changed value against itself and agreed. Replaced with a recording undo, a failure-path test, and an isolated round trip. (3) Two mutations came back caught by `<collection error>` rather than by a named test: `monkeypatch.setattr(os, 'name', 'posix')` patches the real module, and `pathlib` reads `os.name` to choose a Path flavour, so a *failing* assertion inside that window makes pytest raise `cannot instantiate 'PosixPath' on your system` while formatting the failure - an INTERNALERROR that aborts the session, names no test and skips everything not yet run. A green run never notices. Replaced with a `not_windows()` helper patching `vision.capture.os`. The mis-specified pair are worth recording too: the reorder mutation moved the path proof past the *factory* call, which reads nothing, so it had to be re-specified past `capture.capture()` - and then five tests caught it. Sources confirmed byte-restored, no `if False` left in `tools/` or `vision/`, harness deleted. |
| 2026-08-25 | Phase 18.5 primitives probe | `sizeof` on the hand-declared `INPUT`, `MOUSEINPUT`, `KEYBDINPUT` unions, then `SendInput(1, array, 8)` with a deliberately wrong `cbSize` | **`sizeof(INPUT)` is 40** on this 64-bit host (MOUSEINPUT 32, KEYBDINPUT 24, `dwExtraInfo` pointer-sized via `wintypes.WPARAM`), so the structure is measured rather than assumed. The wrong-`cbSize` call **returned 0 and left `GetLastError()` at 0** - no exception, no error code, nothing inserted. The accepted-event count is therefore the only signal that exists for that entire family of failure, which is why `_send` returns it and `_sent` compares it against what was submitted. |
| 2026-08-25 | Phase 18.5 clamp probe | `SetCursorPos` to `(2420, 1580)` and to `(-40, -40)` on a 1920x1080 desktop, position re-read with `GetCursorPos` | **Both returned TRUE and landed somewhere else** - `(1919, 1079)` and `(0, 0)` respectively. In-bounds moves are exact. Section 11's "verification must not rely only on: the command executed without throwing" is the literal failure mode of the primary API here: a click aimed off screen lands on whatever occupies the corner and the caller is told it worked. Hence `_target` refuses off-desktop coordinates **before** moving, and `verify` re-reads the cursor afterwards with a 2 px tolerance - measured exact, slack for a coarser host, not enough to hide a clamp. |
| 2026-08-25 | Phase 18.5 key-state probe | `GetAsyncKeyState(VK_SHIFT) & 0x8000` before, between and after a synthesized shift down/up pair | **A synthesized modifier is visible to the API that reads modifiers**: false before, true after the down event, false again after the up. That is what makes `press_keys`'s stuck-modifier assertion a genuine postcondition rather than a decorative one - and the failure it catches is nasty, because a CTRL that never came back up turns everything the owner types next into shortcuts with nothing anywhere saying why. |
| 2026-08-25 | Phase 18.5 UIPI honesty check | Microsoft's `SendInput` documentation, read for what the return value covers | **Input blocked by UIPI is reported through neither the return value nor `GetLastError`.** A full accepted count therefore does **not** prove arrival at an elevated window. Recorded rather than tested, because closing it needs something running as administrator; `_sent`'s message names the possibility instead of claiming delivery. |
| 2026-08-25 | Phase 18.5 end-to-end oracle | the production tools driving a tkinter window we own, reading back the widget's own contents rather than a clipboard copy | **Six oracles, FAILURES: none.** Text byte-identical including a newline and a tab; backspace deleting exactly one character; `shift+q` producing `Q`, which is what proves the release ordering; the click landing on the exact aimed pixel with the window correctly named; both refusals firing; the clamp reproduced. The probe was meant to drive Notepad and its pre-check found **`cf-backup-codes.txt - Notepad` already open** - the owner's own file, on the machine the suite runs on - so it refused and Notepad was abandoned entirely. The `window` guard justified itself on its first real run, unprompted. Two smaller findings: Windows 11 Notepad is a packaged app so the pid `subprocess` returns does not own the window, and `GetClipboardData` **must** declare `restype = wintypes.HANDLE` or `GlobalLock` on the truncated handle **segfaults the process** (exit 139, buffered stdout lost - run probes with `-u`). |
| 2026-08-25 | Phase 18.5 registration | `build_registry` against the shipped `config.yaml` and against a full grant, plus `ToolPolicy.from_config` | Shipped config registers **9** tools including all four input tools, and `tools.allowed` is **`['current_time', 'remember']`** with `auto_approve` at SAFE only - so registering them grants nothing and gate three is what stands between a registered tool and a running one. A full grant registers **18**. Section 2 both ways: the owner can enable these freely by name, and does not find them already enabled without having said so. |
| 2026-08-25 | Phase 18.5 unit tests | `.venv/Scripts/python.exe -m pytest tests/test_input.py -q` | **161 passed.** Built around three falsifiable properties rather than line coverage: a refused call sends nothing (asserted on the mock's recorded events, so a reordering that validates *after* acting fails even with an identical message), the pointer's position is re-read rather than assumed, and a stuck modifier is caught. `TestTheWindowsBackend` runs on real hardware and **never presses a key or a mouse button** - the owner may be using this machine - with one pointer move restored in a `finally`. |
| 2026-08-25 | Phase 18.5 full suite | `.venv/Scripts/python.exe -m pytest -q` | **`3059 passed, 3 skipped, 1 deselected in 44.84s`** - 161 more than 18.4's 2898, which is exactly `tests/test_input.py`, and no regressions. No new config keys, so no Android fixture change and nothing for the settings contract to learn. |
| 2026-08-25 | **Phase 18.5 mutation** | 45 hand-built mutations over `tools/builtins/input.py` and `tools/factory.py`, each attributed to a named test | **45 of 45 caught, 0 survivors, 0 misapplied** after fixes, and it found **three real gaps rather than none**. (1) `test_they_share_the_window_source_with_the_desktop_tools` patched `default_window_source` with `lambda: source` - one shared object - so it passed whether the factory called it once or five times, and the mutation giving the guard its own source **survived**. Replaced with a stub handing out a *different* `FakeWindows` per call, asserting the count is 1 and cross-checking against `list_windows` (which stores it as `self.source`, not `windows`). (2) `press_keys`'s description said neither "real" nor "in front" while `click_mouse` and `type_text` both do, and it is the tool that can send `alt+f4`; the description is what a confirmation prompt shows, so the fix went in the description, not the test. (3) **The backend's event stream had no unit coverage at all** - the chord release order is Windows-only code the mock cannot speak for, and it holds the worst failure in the file, because releasing CTRL before the letter turns `ctrl+s` into a stray `s` typed into the document. The e2e probe caught that on hardware; nothing caught it in the suite. Added `TestTheEventsSentToWindows`, stubbing `_bind` truthy and replacing `_key_event` with a recorder, so the order, the KEYUP flags, the extended flag on *both* surrogate halves and the UTF-16 pair are asserted without a keyboard; seven mutations now land there. The two initially-misapplied patterns were informative too: `risk = ToolRisk.DANGEROUS` appears **once**, on the shared `_InputTool` base, so a single edit downgrades all four tools, and `MoveMouseTool` declares x and y identically to `ClickMouseTool` so the click block needed its `button` parameter as an anchor. Two defects the writing itself found are also in the battery: the mock's accepted count cancelled itself by operator precedence, and `split_typing` collapsed a blank line. Sources confirmed byte-restored, harness deleted. |

| 2026-08-26 | Phase 19.1 chain tests | `.venv/Scripts/python.exe -m pytest tests/test_vision_chain.py -q` | **25 passed.** Both decline modes exercised against the real classes rather than stand-ins: `OllamaVisionProcessor` with `urlopen` patched to raise `URLError` (returns `""`), and `CloudVisionProcessor([])` with no providers (asserted to *raise*). A chain that caught only one of those would go silent for one backend, which is the bug 19.1 exists to fix, reintroduced a layer up. Also pins first-non-empty by asserting the second processor's `calls == 0`, warned-once-then-debug on `caplog.records` levelno counts, that the built chain is exactly `[Ollama, Cloud, WindowTitle]`, and that a provider key is not permission to upload a screenshot (parametrized). |
| 2026-08-26 | Phase 19.1 regression fixed | `tests/test_vision_settings.py::test_the_desktop_processor_is_built_with_the_ollama_tag` | **1 failed, 3058 passed** - the test asserted `processor.model` on what `_build_vision_processor` now returns, which is a `ProcessorChain` with no `.model`. Fixed by reaching through `chain.processors` for the single `OllamaVisionProcessor` rather than weakening the assertion: the regression it exists for - the *cloud* model name being handed to a local daemon - is exactly as possible one layer down. `19 passed in 2.72s` after. |
| 2026-08-26 | Phase 19.1 Android | `./gradlew cleanTestDebugUnitTest testDebugUnitTest --no-build-cache --console=plain` | **BUILD SUCCESSFUL in 59s**, exit 0. `compileDebugKotlin`, `compileDebugUnitTestKotlin` and `testDebugUnitTest` all listed **without** UP-TO-DATE or FROM-CACHE, which is what section 34 requires before the word verified: `VisionConfigDto`'s three new fields, the hand-edited live fixture (`configurable: 59`) and `SettingsContractTest`'s new value assertions were genuinely executed. |
| 2026-08-26 | Phase 19.2 tool tests | `.venv/Scripts/python.exe -m pytest tests/test_vision_tool.py -q` | **29 passed.** Six groups. Shape: `inspect.signature` equality between `execute` and `verify` (a drifted pair turns every success into "ran but could not be verified"), `ToolProtocol` isinstance, empty `parameters`. Execute: observes now, proven with a fake clock left *inside* the throttle and a changed window title, so a `get_context()` mutation is caught rather than passing on a coincidence; refuses with `vision.enabled` named and - the half that matters - **does not observe**, asserted on the mock processor's `calls == 0` and both manager readings being None. Verify: ok after a real execute, fails when nothing was observed, fails past `STALE_AFTER` (imported, not written down), still passes one second inside it, fails on a fresh-but-dropped observation, and does not re-observe with `min_interval=0` (`calls == 1` before and after). Risk: SENSITIVE for titles/mock/local chain and for a processor that never heard of the flag, DANGEROUS for `CloudVisionProcessor([])` bare *and* buried mid-chain, surviving `ToolRegistry.register`. Registration: absent for no manager and for a disabled one, present with the other builtins intact. Executor: an approved call returns the description; registered-but-not-allowed and allowed-but-unapproved both return the exact gate message **and never capture**; a stale verify downgrades a successful execute end to end. |
| 2026-08-26 | Phase 19.2 caplog defect | full-suite run | **Found by running the whole suite, not the file.** `test_vision_switched_off_means_no_tool` passed alone and failed in the suite: under pytest the root logger already has a handler when `core.logger` is imported, so `setup_logger` returns early on `hasHandlers()` and never calls `setLevel` - measured, `Aura.level == 0` (NOTSET) - but any earlier test that runs `apply_config_level` leaves it at INFO, and at INFO the factory's `logger.debug` is dropped **at the logger**, before caplog's root handler sees it. A bare `caplog.at_level("DEBUG")` only raises root's level. Fixed in the test with `logger="Aura"`, the idiom `tests/test_error_visibility.py` already uses; the source was not touched, because the source is right. |
| 2026-08-26 | **Phase 19.2 mutation** | 15 hand-built mutations over `tools/builtins/vision.py`, `tools/factory.py`, `vision/processor.py`, `vision/cloud_processor.py` and `vision/manager.py`, each named against the test that must notice | **15 of 15 caught, 0 survivors.** Every anchor asserted to match exactly once before writing, and all five files restored from an in-memory snapshot in a `finally`. The security-relevant four: dropping the `vision.enabled` guard from `execute` (section 2) is caught by the tool test; making `CloudVisionProcessor.sends_pixels_offsite` False (section 30) and fixing the risk at SENSITIVE are both caught by the cloud risk test; making the chain report only its *first* link's exposure is caught by the mid-chain test - the shipped order puts the cloud link in the middle, so a first-link reading would have understated a live upload. Section 11's three: ignoring staleness, returning ok unconditionally, and reading `get_context()` instead of `last_observation` are each caught by a different verify test. One mutation initially reported ANCHOR x2 rather than surviving - `return self._context` appears twice in `manager.py` - and was re-run with a docstring-tail anchor: caught by `test_an_observation_just_inside_the_bound_still_verifies`. |
| 2026-08-26 | Phase 19 full suite | `.venv/Scripts/python.exe -m pytest -q` | **`3113 passed, 3 skipped, 1 deselected in 42.61s`** - 54 more than 18.5's 3059, which is exactly `tests/test_vision_chain.py` (25) plus `tests/test_vision_tool.py` (29), and no regressions. |
| 2026-08-26 | Phase 20 audit | audit-only; `pytest tests/test_voice*.py tests/test_cli_voice.py -q` | **127 passed in 0.60s.** Verdict: PARTIALLY IMPLEMENTED — DEFERRED BY OWNER (push-to-talk + event TTS wired and proven; streaming speech, interruption trigger, continuous listening, hosted-TTS offsite disclosure = recorded debt, row 20). |
| 2026-08-26 | Phase 21 plugins | `.venv/Scripts/python.exe -m pytest tests/test_plugins.py -q` then full suite | Focused suite green after closing the composition-root test gap (`build_services` wires real `PluginManager`, builtin registers through the factory); full suite green. |
| 2026-08-26 | Phase 22 diagnostics | `.venv/Scripts/python.exe -m pytest tests/test_server.py tests/test_settings_contract.py -q` then full suite | Plugin/tool status added to `/api/health` via `_plugins_label`/`PluginManager.summary()` naming what failed to initialize, not a bare count; focused suites green, full suite green. |
| 2026-08-26 | Phase 23 settings/UI | backend settings contract suite + `gradlew :app:compileDebugKotlin` + `:app:testDebugUnitTest` | 14 settable-without-widget paths closed with Hub controls (companion knobs, vision capture/cloud/interval, timezone, task lanes, custom endpoint); DTOs defaulted so older servers parse. Android: 27 classes, 378 tests, 0 failures/errors/skipped. |
| 2026-08-26 | Phase 24 perf baseline + fix | timed `load_config` before/after; `.venv\Scripts\python.exe -m pytest tests/test_config.py -q` | Baseline ~10.5 ms/call (yaml.safe_load 6.8 ms of it), called several times per turn → cached merge keyed on stat+overlay token+version, deepcopies out: **0.077 ms/call (~136×)**. 36 config tests pass incl. 5 new invalidation tests. |
| 2026-08-26 | Phase 24/25 full suite | `.venv\Scripts\python.exe -m pytest -q` | **3124 passed, 3 skipped, 1 deselected in 53.42s** — no regressions against phase 23's baseline. |
| 2026-08-26 | Phase 25 RC audit | git status sweep; plugin wiring trace; Android XML freshness check | Plugins reach the runtime via `_build_plugins` in the composition root and are named in diagnostics; no dead required paths introduced by 21–24; Android test XML (UTC 01:37) postdates the last `.kt` edit (UTC 01:36) across UTC+7. |

## Preserved pre-existing uncommitted work (do not revert)
- `android/.../AuraAccessibilityService.kt` (405 changed lines, open_app verification race + action safety)
- `android/app/src/test/.../AgentLifecycleTest.kt` (untracked)
- `local_agent/` (untracked: Qwen3-Coder local agent harness + its own .venv)
- `android/aura-agent*.log` (untracked capture logs)
- `da` (untracked 19KB stray capture of `git diff` output; left untouched)
