<?xml version="1.0" encoding="utf-8"?>
<!--
    RELEASE network policy: HTTPS only.

    A release build of Aura will not send a token or a message in
    cleartext to anything, with two exceptions that cannot be reached from
    the Internet: the emulator's host loopback and localhost.

    Home-network use over plain HTTP is a *debug* configuration. See
    `app/src/debug/res/xml/network_security_config.xml`. The split is
    deliberate: an IP-literal range like 192.168.x.x cannot be expressed
    here at all - Android matches `domain` entries by DNS label suffix,
    which does not expand IP ranges - so the honest options are "permit
    cleartext everywhere" or "permit it only in debug builds". This is the
    second.

    Note what is absent: any user-added CA trust. Trusting user CAs is a
    common shortcut for proxy debugging and it means an installed profile
    can read every message and the bearer token.
-->
<network-security-config>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- The Android emulator's alias for the host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

</network-security-config>
