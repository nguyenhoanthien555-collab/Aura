<?xml version="1.0" encoding="utf-8"?>
<!--
    Nothing leaves this device in a backup or a device-to-device transfer.

    `allowBackup="false"` in the manifest already covers this, but the rules
    are stated explicitly because the encrypted settings store holds the
    bearer token, and a token that survives into a cloud backup is a token
    that has left the Keystore that was protecting it.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
