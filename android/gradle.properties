org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# No secret belongs in this file. It is committed. The Aura server URL and
# bearer token are entered by the user at first run and stored in
# EncryptedSharedPreferences on the device - never baked into the APK.
# See docs/SECURITY.md.
