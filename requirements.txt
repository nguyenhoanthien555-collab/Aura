# Core. Aura runs as a text companion with nothing but these.
rich
python-dotenv
pyyaml
# Timezone database for `zoneinfo`. Windows does not ship a system tz
# database, so every IANA name - "Asia/Ho_Chi_Minh" included - fails to
# resolve without this package; Linux images have one and ignore it.
# Harmless where the OS already provides the data: zoneinfo only falls
# back to it when the system lookup misses.
tzdata>=2024.1
# Floor, not decoration: releases before 2.0.51 cannot map a
# `Mapped[str | None]` column on Python 3.14. `memory/models.py` has three
# of them, so an older SQLAlchemy on a newer interpreter is not a
# degraded Aura, it is one that will not import.
sqlalchemy>=2.0.51
google-genai
pillow
pytest

# ----------------------------------------------------------------------
# Server mode (launcher.py --server, Docker, the Android companion)
#
# Desktop mode never imports these. They are listed as hard requirements
# rather than extras because `server/` and `tests/test_server.py` cannot
# degrade gracefully without them - there is no fallback for "no HTTP".
#
# Versions are floors, not pins. The deployed image pins exact versions
# in requirements-server.txt; see docs/DEPLOYMENT.md.
# ----------------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
httpx>=0.27
websockets>=12.0

# ----------------------------------------------------------------------
# Optional extras
#
# Every one of these is imported lazily, inside the function that needs
# it, and falls back to a mock when missing. Install only what you want.
#
#   edge-tts         Aura's intended voice    voice.tts.provider: edge
#   sounddevice      microphone capture       voice.stt.enabled
#   faster-whisper   local speech recognition voice.stt.provider: whisper
#   pyttsx3          cross-platform speech    voice.tts.provider: pyttsx3
#   mss              faster screen capture    vision.capture_screen
#
# Screen capture needs nothing from this list. `mss` is used when it is
# present and `vision.capture.GdiScreenCapture` - ctypes against user32
# and gdi32 - is used when it is not, so `vision.capture_screen: true`
# works on a stock Windows machine. Pillow is a hard requirement above
# rather than an extra, because the vision processors encode with it.
#
# The line this replaces said vision required BOTH mss and pillow and
# that without them vision fell back to window titles. Neither half was
# true after phase 18.4 added the GDI backend, and the fallback is now a
# chain rather than a substitution: with `capture_screen: true` and no
# reachable vision model the window title description is still what
# comes out.
#
# Windows speech output needs nothing extra: voice.tts.provider "auto"
# uses the SAPI voices already on the machine.
#
# edge-tts is the one worth installing on purpose - warm, expressive
# neural voices, no API key. It does need a network round trip per reply,
# which is why "auto" never picks it. Playback uses PowerShell on Windows
# and ffplay/mpv/afplay elsewhere; `playsound` is used when installed.
#
# The avatar needs tkinter, which ships with the official Python
# installer. Without a display it degrades to a null renderer.
# ----------------------------------------------------------------------
